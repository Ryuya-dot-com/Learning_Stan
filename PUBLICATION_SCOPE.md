# 公開範囲ポリシー

最終更新: 2026-08-09

このプロジェクトでは、「公開」を一つの意味で扱いません。GitHub上のソース公開、GitHub Pagesの学習アプリへの掲載、規定観察での参加者配布を分けます。

## 1. GitHubソースとして公開するもの

次は、完成度が途中でも、状態と限界を明示したうえで公開できます。

- アプリのソース、テスト、CI、ビルド設定
- 公開中レッスンと、`draft-unpublished`を明記した公開前原稿
- 合成データ、再現用スクリプト、検証結果、SHA-256
- 理解問題、rubric、model answer、facilitator keyなど、教材設計を再現・レビューするための資料
- 個人情報が一切入っていない観察手順と空の記録票

このリポジトリは公開教材の開発過程も透明にする方針です。そのため、GitHubにある採点基準や参照解答は秘密ではありません。`facilitator`は「通常の参加者用ZIPへ入れない」という配布上の役割を表し、GitHub上の機密性を表しません。

## 2. 学習アプリへ掲載するもの

GitHubに原稿があっても、学習アプリへ自動的には掲載しません。

現在の公開アプリ範囲は、体験、STEP 0、R基礎、Foundation Check、STEP 1（L1–L16）です。STEP 2とStan編はソースレビュー可能なドラフトですが、各公開ゲートを通るまでアプリの学習導線へ加えません。

Stan編は、[Stan Release Gate](quality/stan-release-gate/README.md)で対象commitの静的検証、クリーンCI、既存runtime再検証、L40実測、公開範囲確認、最終判断、未解決P0・P1がないことを確認してから、ベータ版として原則1レッスンずつ公開します。したがって、現時点のL34–L41は「GitHubでは閲覧可能、学習アプリでは未公開」で、ゲート判定は`BLOCKED`です。`SRG02`〜`SRG05`は`PASS`ですが、所有者による公開範囲確認と最終判断が未完了です。Foundation Gate、第三者の感想、初学者観察、7〜14日後の遅延保持は公開後も続ける改善証拠であり、未実施時は第三者検証済み・学習効果確認済み・保持効果確認済みとは表示しません。

## 3. 公開しないもの

次はGitHubへコミットしません。

- 入力済みの観察記録、同意に関する記録、個人を推測できる自由記述
- 生の画面録画・録音・スクリーンショット・コンソール履歴
- 氏名、メール、所属、学籍番号、端末ユーザー名、研究データ
- API key、token、password、認証ファイルなどのsecret
- 公開済み解答を見ていないことが要件となる観察で使う、未見の課題variantと採点鍵

実観察の成果物は各gateの`private/`、`raw/`、`recordings/`、`filled-records/`のいずれかへ置きます。初回提出用の未見variantは`content/**/private-variants/`へ置きます。これらは`.gitignore`で公開対象から除外します。

## 4. 公開済み解答と規定観察

公開リポジトリの解答やrubricを事前に見た参加者の結果を、未見転移・初回想起の証拠には使いません。観察時は、次のどちらかを記録します。

1. 参加者が公開解答を見ていないことを確認し、解答を除いたparticipant packだけを配る。
2. 公開版と同じ到達目標・難易度を持つ非公開variantを作り、初回提出を固定してから採点する。

教材の自己学習では公開解答を利用できますが、それは統制された初回観察とは別の証拠です。

## 5. コミット前チェック

- `git diff --cached`で公開対象をファイル単位に確認する
- `npm run build && npm run audit:stan-public-scope`でGit管理対象とアプリ成果物を検査する
- secret、直接識別子、入力済み記録がないことを検索する
- `draft-unpublished`と「GitHub非公開」を混同していないか確認する
- participant packに採点鍵・自己チェッカー・完成コードが混入していないことをテストする
- 公開アプリの導線が、通過済みgateの範囲を越えていないことをテストする

`package.json`の`"private": true`はnpmレジストリへの誤公開を防ぐ設定であり、GitHubリポジトリの公開・非公開を決める設定ではありません。
