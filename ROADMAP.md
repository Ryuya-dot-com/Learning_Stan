# Learning_Stan 改善・発展ロードマップ

最終更新: 2026-08-10
対象: 現在公開中の L1–L16 と、意味IDで設計中のL17以降・ブリッジ・補講の将来構想

## 0. この文書の役割

この文書を、品質改善と教材拡張の実行計画に関する正本とする。

- `public/roadmap.html`: 学習者向けに「何を、どの順で学ぶか」を示す公開ページ
- `ROADMAP.md`: 開発者向けに「何を、なぜ、どの順で直し、何をもって完了とするか」を示す正本
- `docs/`: 過去の設計・実装過程を残すローカル資料。意思決定の正本にはしない

公開ロードマップは、将来的に構造化データから生成し、手動二重管理を廃止する。

## 1. 北極星と成果の定義

### 北極星

プログラミング未経験の学習者が、自分の研究データをRで読み、整え、可視化し、統計モデルを作り、診断し、不確実性を含めて説明できるようになる。

### 修了状態は二層に分ける

| 状態 | 意味 | 証拠 |
|---|---|---|
| 理解済み | 説明を読み、概念・出力予測問題に正答した | Webアプリ内の演習 |
| 実践済み | 自分でコードを実行し、結果を再現・解釈した | R / Quarto の実践課題 |

Webアプリの正答だけを、実技修得の証拠として扱わない。

### 現段階の非目標

- サーバ側アカウント・学習履歴収集
- 成績評価や試験監督
- webRによる全面的なブラウザ内R環境
- すべての統計手法を網羅すること
- STEP 2以降の公開量産を、STEP 1の対象者観察とFoundation Gateより優先すること

## 2. 意思決定台帳

重要な方針は、暗黙の前提にせずここで管理する。

| ID | 論点 | 現在の判断 | 状態 | 再検討条件 |
|---|---|---|---|---|
| D-001 | 最終KPI | 「読了」ではなく「自力で分析・診断・説明できる」を推奨 | 要確認 | プロジェクト所有者が主目的を確定したとき |
| D-002 | 修了の扱い | 理解済み / 実践済みの二層 | 採用・実装済み | 実践課題の運用負荷が許容できないと判明したとき |
| D-003 | 進捗保存 | サーバ送信なし。バージョン付き `localStorage` と手動消去・書き出し | 採用・実装済み | 共有端末での利用が主要用途になったとき |
| D-004 | コード実行 | 当面はローカルR / Quartoを正とし、webRは導入しない | 暫定採用 | 環境構築による離脱が実測で主要因になったとき |
| D-005 | 学習順 | 体験(L1) → STEP 0(L10本文・理解) → R基礎(L2–L9) → Foundation Check(L10実機) → STEP 1(L11–L16・成果物) | 採用・実装済み | 初学者観察で重大な順序問題が見つかったとき |
| D-006 | 正本 | 本文書を改善計画の正本とし、公開ロードマップは別目的に限定 | 採用 | 構造化データから両方を生成できるようになったとき |
| D-007 | 新規執筆 | L17以降は非公開設計・実行検証を進めてよいが、公開組込みはFoundation Gate、STEP 1データ品質観察、STEP 1独立転移観察の所定判定まで凍結する | 再改訂・採用 | STEP 1の対象者観察とM5の完了判定時 |
| D-008 | 進捗の前方互換性 | 壊れたデータ・未知の将来schemaは自動上書きせず、既知schemaだけを教材IDと問題範囲で正規化 | 採用 | サーバー同期を導入するとき |
| D-009 | 共有URL | GitHub Pagesでサーバー側fallbackを要しないhash routingを採用 | 採用 | 独自サーバーまたはSSRへ移行するとき |
| D-010 | 応用文脈 | STEP単位で1つの具体的な分析依頼を通し、各レッスンの役割・最終成果物・解釈限界を明示する | 採用・STEP 1実装済み | 対象者観察で文脈の反復が認知負荷になる、または別領域への転移を妨げるとき |
| D-011 | 未公開範囲の順序 | 意味に基づく恒久IDと暫定表示番号を分離する。STEP 2の後に回帰ブリッジを置き、階層・二値GLMM・反応時間モデルを分割してからStan編の公開番号を確定する | 採用・STEP 2設計へ適用済み | 公開用ID移行規則または正本生成方式を実装するとき |
| D-012 | Stan構文の習得 | Stan構文をL35の単発説明にせず、STEP 5の生成コード読解からL41後の遅延転移まで続く必須の「Stan Syntax Spine」とする。言語・確率モデル・計算の三層を、読解・穴埋め・変更・デバッグ・白紙再現・遅延転移で反復する | 採用・非公開設計へ適用 | 初学者観察で反復量が認知負荷を増やす、または別の間隔・順序の方が保持と転移を改善すると判明したとき |

## 3. 品質を測る軸

### 必須の品質指標

| 軸 | 指標 | リリース条件 |
|---|---|---|
| 正確性 | 既知の重大な事実誤認 | 0件 |
| 評価妥当性 | 到達目標に対応する評価証拠 | 全レッスンに存在 |
| 再現性 | 自動検証可能なR例のCI成功率 | 100% |
| 測定整合性 | 初見・再挑戦・修了状態のUIテスト | 全必須経路が成功 |
| アクセシビリティ | キーボード完走、フォーカス、読み上げ上の重大問題 | 0件 |
| セキュリティ | 既知の依存脆弱性、過大なCI権限 | 重大0件、権限はjob単位最小化 |
| 保守性 | 正本の重複 | 手動同期が必要な正本は0組 |

### 学習効果の指標

サーバ側テレメトリは前提にしない。対象学習者による観察テストと、匿名化した任意アンケートで評価する。

- 説明を見ずに類題を解けるか
- 学習例と異なる列名・数値でもコードを書けるか
- エラーから原因を特定できるか
- 出力を研究上の言葉で説明できるか
- 数日後に同じ技能を再現できるか
- 環境構築で必要になった支援の回数と種類

数値目標は、最初の対象学習者テストを基準値としてから固定する。根拠のない達成率を先に置かない。

## 4. クリティカルパス

```text
M0 現状固定・判断確定
  ↓
M1 正確性と測定の緊急修正
  ↓
M2 自動検証・CI・UIテスト基盤
  ↓
M3 進捗・URL・アクセシビリティ
  ↓
M4 カリキュラム再設計
  ↓
M5 L1–L10の再整備 ── Foundation Gate
  ↓
M6 STEP 1以降を段階公開
  ↓
M7 学習効果の検証と継続改善
```

M1の事実訂正だけは、D-001の最終決定を待たずに着手できる。

## 5. マイルストーン

### M0 — 現状固定と意思決定

目的: 修正中に評価基準が動かないようにする。

作業:

- D-001「読了か実技修得か」を確定する
- 現在のL1–L10、30演習、35説明ページを基準状態として記録する
- 既知問題を重大度・影響範囲・再現手順付きで課題化する
- 公開中、構想中、実装済みを文言上明確に分離する
- 変更前の主要操作をスクリーンショットまたは手順書で残す

完了条件:

- D-001〜D-007の状態が明示されている
- P0/P1課題に所有者、依存関係、受入条件がある
- 新規教材執筆の凍結範囲が合意されている

### M1 — 正確性と測定の緊急修正

目的: 学習者へ誤概念や誤った達成情報を返さない。

状態: **実装範囲は完了・KPI確認待ち**。2026-08-01に事実訂正・初見判定・外部リンク・公開範囲表記を修正し、「修了」を「理解済み / 実践済み」へ分離した。最終KPIはD-001に基づき、対象学習者の観察後に確定する。

対象:

- `logical` と `NA` の説明・演習を訂正する
- 因子水準をコードで明示し、ロケール依存を除く
- コンソール履歴など断定的説明を精査する
- 誤答後のページ移動で「初見」に戻る問題を修正する
- L10のCRAN・Positリンクを実リンクにする
- ホーム、README、メタ説明を現在公開中の範囲に合わせる
- 「修了」と「一発クリア」の表示条件を再定義する

完了条件:

- 上記各問題に失敗→修正成功を示すテストまたは再現記録がある
- `SOURCES.md` に基礎Rの主要な事実主張も追加されている
- 学習者がクリック操作で公式配布ページへ到達できる
- 「初見」は、画面移動・ホーム復帰後も誤答履歴を失わない

### M2 — 自動検証と安全なCI

目的: 人手レビューだけに依存せず、誤りの再発を止める。

状態: **実装完了**。React UIテスト、分類付きR実行検証器、workflow構文テスト、base branchを問わないPR時のtest/build/R/Stan検証、job単位の権限分離、ActionsのSHA固定、Node 22.23.1 / npm 10.9.8固定を実装した。Pages公開は`main`へのpushだけに限定し、2026-08-10の[GitHub Actions run 31323415009](https://github.com/Ryuya-dot-com/Learning_Stan/actions/runs/31323415009)でbuild・R・Stanの3 job成功とPR時deploy skipを確認した。

作業:

- Reactコンポーネントテスト環境を追加する
- 次の必須シナリオをテストする
  - 初回正解
  - 誤答→正解
  - 誤答→ページ移動→復帰→正解
  - 修了済み問題への再訪
  - リセット
  - 最後のレッスンと番号なしトラックの遷移
- レッスンファイル名、ID、ページ、コード、出力、リンクのスキーマ検査を強化する
- R実行用の検証器を導入する
  - `exact`: 文字列を厳密比較
  - `numeric`: 許容誤差付き比較
  - `stochastic`: シード・環境情報・範囲で比較
  - `manual`: OS固有画面など、自動化不能理由を明記
- CIを `pull_request` でも実行する
- buildとdeployの権限を分離する
- GitHub Actionsを完全なコミットSHAに固定する
- Node/npmの対応バージョンを固定する

完了条件:

- クリーン環境で `npm ci` → test → build が成功する
- 代表的なR例がCI上で再実行される
- build jobはPages書き込み・OIDC権限を持たない
- 既知の重大な操作経路が自動テストで保護されている

### M3 — 継続学習とアクセシビリティ

目的: 長期コースを、端末や操作方法にかかわらず継続できるようにする。

状態: **自動化範囲は実装済み・実機監査待ち**。schema v2、v1からの移行、教材版移行、破損・将来版の上書き防止、理解・実践進捗のブラウザ保存、JSON入出力、hash routing、履歴復元、動的title、見出しフォーカス、進捗bar semantics、キーボード完走テスト、axe重大違反検査を実装した。NVDA / VoiceOverと実ブラウザでの色コントラスト確認は未完了。

作業:

- 進捗スキーマv2を定義し、v1を移行する
  - `schemaVersion`
  - `contentVersion`
  - 理解済み問題
  - 初回誤答の有無
  - 実践課題の自己確認結果
- `localStorage` 保存、明示的消去、JSON書き出し・読み込みを実装する
- 古い進捗の移行・破棄ルールを定義する
- URLまたはハッシュでホーム、チートシート、各レッスンを表現する
- 戻る・進む・再読み込みを自然に扱う
- `<main>`、見出し、進捗バー、選択問題のセマンティクスを整える
- 画面遷移時に見出しへフォーカスを移す
- TF問題にも一貫した正誤通知を実装する
- キーボードのみで全レッスンを完走する検査を追加する

完了条件:

- リロード後も進捗が維持される
- 共有URLから目的のレッスンを直接開ける
- 保存内容がサーバ送信されないことをUI上で説明している
- キーボードと読み上げの重大な阻害要因がない

### M4 — カリキュラム再設計

目的: 未経験者が無理なく、概念理解から実践能力へ進む順序を作る。

推奨する学習順:

1. インストール不要の短い体験版
2. STEP 0: R / RStudio導入と動作確認
3. R基礎: 読む→予測する→実行する
4. データ操作と可視化
5. 線形回帰の橋渡し
6. 確率、生成過程、ベイズ推定
7. brmsによる回帰
8. 階層モデル
9. GLMMと反応時間モデル
10. 生Stanと卒業制作

追加・分割する概念:

- `lm()` とフォーミュラ
- 切片、係数、残差、予測
- 因子の参照水準とコントラスト
- 交互作用
- 標準化と事前分布のスケール
- 尤度とデータ生成過程
- モデルの仮定、失敗例、感度分析
- 階層モデルとnon-centered parameterization
- 二値GLMM
- 反応時間モデル

現行L33の「階層モデル・GLMM・反応時間分布」は複数レッスンへ分割する。

完了条件:

- 全レッスンに前提知識と後続依存が定義されている
- 未習概念を前提とするレッスンがない
- 各STEPに少なくとも1つの実践成果物がある
- 公開ロードマップが新しい順序・公開状態を正確に反映する

### M5 — L1–L10の再整備

目的: 将来の全レッスンが倣える模範実装を、現在の10本で確立する。

状態: **自動検証・L10実践導線・実施プロトコル・機械判定は実装済み、実機監査と学習者観察待ち**。全10目標について、直接評価証拠が目標の全次元を覆う構造化対応表を導入した。L1には保存しない記述式の自己説明、L5–L9にはコード産出演習、L10には5項目の実機チェックを追加した。実施手順と匿名記録票は[Foundation Gate実施キット](quality/foundation-gate/README.md)を正本とし、現在の機械判定は`BLOCKED`（PASS 0/14）である。

初心者導線は[UX-PATH-001](quality/beginner-journey/README.md)を正本とする。教材IDと保存形式を変えず、L10の本文・理解問題をSTEP 0としてL1直後に置き、L10の実機確認をL2–L9後の独立したFoundation Check画面へ分離した。ホームは現在地と次の一手を先に示し、全教材一覧を補助導線へ移した。

各レッスンの基本構造:

1. 到達目標と前提
2. 最小の worked example
3. 実行前の出力予測
4. 実行または手順確認
5. 誤りやすい例
6. 概念問題
7. 産出問題
8. 別文脈への転移問題
9. 要点と次への接続

L10の実践確認:

- Rが起動する
- `1 + 1` を実行できる
- R Scriptを作成・保存できる
- スクリプトからコードを実行できる
- RStudio Projectを作成できる
- `install.packages()` と `library()` の役割を区別できる

完了条件 — Foundation Gate:

- L1–L10が新しいレッスン完了定義を満たす
- すべての自動検証が成功する
- 対象学習者による完走観察を実施している
- 重大な事実誤認、操作不能、到達目標との不整合がない
- 進捗保存と削除の挙動が説明どおりである

判定は[Foundation Gate判定票](quality/foundation-gate/GATE_DECISION.md)に従う。自動テスト成功、実施予定、証拠ファイルの存在だけでは通過とせず、必須構成の実機監査、適格な初学者3名以上の全必須タスク、未解決P0・P1が0件であることを確認する。

### M6 — STEP単位の段階公開

状態: **STEP 1公開済み・対象者観察待ち、STEP 2非公開設計中**。L11–L16、形式別サンプル、問題入りCSVによる品質ゲート、NB1、成果物チェック、到達目標―評価対応、Rパッケージ込みのCI検証を公開した。個別ファイルの配置で初学者が脱落しないよう、展開後のProject構成・案内文・全入力を固定した一括スターターZIPも追加した。初学者からの非公式フィードバック「分かりやすいが、実際の分析シナリオがほしい」を受け、6レッスンを認知課題パイロットの依頼、品質確認、条件差の記述、CSVと限界付き結果メモの納品で接続した。さらに、中心課題の列名や完成コードの記憶だけで通過しないよう、列名・条件名・参加者数を変えた注意切替パイロットの独立転移課題をL16後に置いた。解答コードは配布せず、原本保全、既知問題5件、検査済み16行、参加者別切替コスト4件、限界付きメモを自己チェッカーが成果物から判定する。この感想と自動検証は有用な形成的入力だが、規定手順による対象者観察の証拠には代用しない。L12→L13とL16後の独立転移について、参加者用課題と期待値を持つ進行役資料を分離した観察キットは実施準備済みだが、どちらも`NOT RUN`である。Foundation Gateの実機監査・対象者観察も未完了であるため、STEP 2は公開せず、観察結果を反映できる非公開パックとして設計・実行検証する。

STEP 2の非公開パックは、24名×2条件×20試行の拡大合成パイロットをL17–L20の単一シナリオとする。L17で試行数と参加者数を分離し、L18で図の文法、L19で参加者内差、L20で保存・選択理由・限界・未見データへの転移を扱う。最終契約は2図・3CSV・探索メモである。L17は3CSVと探索メモ、L18はL17の48行CSVから2箱・48参加者点・軸・単位・合成データ注記を持つPNGを空の一時Projectから再生成する。公開番号とは別に意味IDを持ち、公開ゲート通過前は`src/data/lessons/`へ置かない。

次の順に進める。

| 段階 | 内容 | 追加ゲート |
|---|---|---|
| 1 | STEP 1 データ操作 | CSVを読み、整形し、保存できる |
| 2 | STEP 2 可視化・記述統計 | 図を実際に生成し、選択理由を説明できる |
| 3 | 回帰ブリッジ | `lm` の係数・予測・仮定を説明できる |
| 4 | STEP 3 確率・シミュレーション | 生成→集計を自力で変更できる |
| 5 | STEP 4 ベイズ基礎 | 事前・尤度・事後を生成過程として説明できる |
| 6 | STEP 5 brms / Stan構文への橋渡し | prior predictive、推定、診断、PPCを一周し、`stancode()`の主要部分を式・生成過程と対応付けられる |
| 7 | 階層・GLMM・RT | データ型に応じた尤度と階層構造を選べる |
| 8 | STEP 6 Stan L34–L35 / 言語の文法 | R / CmdStanR / Stanの責務、データ契約、ブロック、スコープ、型、次元、添字、制約を説明し、最小プログラムを構文確認できる |
| 9 | STEP 6 Stan L36–L37 / 確率モデルの文法 | 生成過程を`~`・`target`・分布関数へ翻訳し、型・次元・ブロック・支持範囲のエラーを修正して再現可能に実行できる |
| 10 | STEP 6 Stan L38–L40 / 計算の文法 | 構文エラー、計算診断、統計的誤指定を分け、generated quantities、数値安定性、変数変換、再パラメータ化を根拠付きで実装できる |
| 11 | STEP 6 Stan L41 / 卒業制作・遅延転移 | 自分の研究設計でモデル・診断・予測検査・報告を統合し、1〜2週間後に別の応答型へ構文と判断を移せる |

各段階の公開実装は、前段階の評価結果を反映してから開始する。非公開ドラフトは、順序・依存・実行可能性を先に検証するために作成できる。

Stan編は[非公開教材パック](content/stan/README.md)でL34–L41の依存関係、直接評価証拠、単回帰の縦切り原稿に加え、8単元32課題の文法スパイラル、分布・リンク関数の可視化、事前予測、ハイパーパラメータ感度、切断・打ち切り、PSIS-LOOモデル比較を先行検証する。現行32課題はSyntax Spineの実行可能な核とし、公開前に型・形状・スコープ・ユーザー定義関数、コンパイラエラー修正、Jacobian、non-centered化、数値安定性を補う。`content/stan/`に原稿が存在しても、Foundation Gateと各前提レッスンを通過するまでは公開レッスン数へ含めない。

#### Stan Syntax Spine — STEP 5からL41後まで続く構文トラック

Stan構文は、単語や記号の暗記ではなく、数式・生成過程・実行時期・型・出力を往復する技能として扱う。構文確認の成功だけでは、モデルの正しさや自力実装を証明しない。

| 層 | 必須範囲 | 主な誤り |
|---|---|---|
| Stan言語の文法 | ブロックと実行順、スコープ、型、array・vector・row_vector・matrix、次元、添字、制御構文、ユーザー定義関数 | 不正なブロック、型・形状不一致、範囲外添字、利用不能な変数・関数 |
| 確率モデルの文法 | distribution statement、`target +=`、分布関数の接尾辞、支持範囲、リンク、制約・切断・打ち切り | `~`を乱数代入と解釈、支持範囲不一致、正規化項欠落、リンク尺度の混同 |
| 計算の文法 | generated quantities、`_rng`、変数変換とJacobian、non-centered parameterization、数値安定性、ベクトル化と計算量 | 許可ブロック違反、Jacobian欠落、悪い幾何、オーバーフロー、意味を変える機械的ベクトル化 |

各単元は、短い接触を`読む・予測する → 穴埋めする → 一部を変える → 意図的なエラーを直す → 見本なしで再現する → 数レッスン後または1〜2週間後に別文脈へ移す`の順で配置する。現行の写経・変更・白紙再現・転移は削除せず、読解、明示的デバッグ、遅延転移を外側へ追加する。

Syntax Spineの公開判定は、次の証拠を別々に要求する。

1. 構文確認・コンパイルが成功する。
2. コンパイラの位置情報と型・次元・ブロック規則から原因を説明して修正できる。
3. 数式・生成過程・Stanコードを相互に翻訳できる。
4. コンパイルは通るが観測過程、支持範囲、正規化、パラメータ化が不適切なモデルを発見できる。
5. 正規モデルの表面だけを暗記せず、二値・件数・正の連続量など未見の応答型へ移せる。
6. 直後ではなく、一定期間後にも白紙再現と転移を再達成できる。

セルフチェックは形成的記録とし、Syntax Spineの通過証拠には、保存されたコード、初回エラー、修正理由、未見転移成果物、遅延再試行を用いる。学習者が公式リファレンスを調べることは許可し、完成例の検索・転記と、仕様を調べて自力で構成することを観察上区別する。

### M7 — 学習効果と運用の検証

目的: 「公開できた」ではなく「学べた」を確認する。

- 対象学習者による観察テスト
- 直後テストと遅延テスト
- 類題への転移テスト
- つまずきログの分類
- OS別の環境構築成功確認
- 教員・統計実務者による内容レビュー
- 四半期または主要依存更新時の事実・リンク再確認

教材の追加速度より、誤学習率・再現不能率・支援なし完遂率を重視する。

## 6. レッスンのDefinition of Done

新規・改訂レッスンは、次のすべてを満たすまで公開しない。

### 内容

- 到達目標が観察可能な行動で書かれている
- 前提知識と後続レッスンが明示されている
- 重要な事実主張に一次情報がある
- 用語・記法を初出時に説明している
- 典型例だけでなく、誤りや境界例を扱っている

### コードと再現性

- 掲載コードが実際に実行されている
- 実行環境と主要パッケージ版が記録されている
- 出力検証方法が `exact` / `numeric` / `stochastic` / `manual` のいずれかで指定されている
- ロケール・OS依存がある場合は固定または注記されている
- サンプルデータが合成か実データか明記されている

### 評価

- 目標を直接測る問題がある
- 少なくとも1問は答えを見た直後の再生だけでは解けない
- 実技目標にはコード産出または実行課題がある
- 正解だけでなく、誤答理由を説明している
- 初見と再挑戦を正確に区別できる

### Stan構文を扱うレッスン

- 数式・生成過程・Stanコード・実行ブロックの対応が明示されている
- 構文エラー、型・次元エラー、計算診断、統計的誤指定を別々に扱っている
- 少なくとも1つの意図的なエラー修正課題と、コンパイルは通る誤モデルの診断課題がある
- 正規モデル以外の応答型または異なるデータ形状への転移課題がある
- 直後の白紙再現に加え、後続レッスンまたは1〜2週間後の累積再試行がある
- セルフチェックと、保存コード・修正理由・転移成果物による実技証拠を区別している

### UIとアクセシビリティ

- モバイルとデスクトップで読める
- キーボードのみで操作できる
- フォーカス順と正誤通知が自然である
- リンクの目的が明確で、リンク切れがない
- 色だけに情報を依存していない

## 7. リスク登録簿

| リスク | 影響 | 予防策 | 発生時の判断 |
|---|---|---|---|
| STEP 1の型を検証せずL17以降を量産する | 誤った型を全教材へ複製 | STEP 1のR実行検証・成果物・対象者観察を先に行う | 新規執筆を止め、STEP 1の観察結果へ戻る |
| webRを早期導入する | バンドル・互換性・保守負荷増大 | ローカルRを既定にする | 環境構築離脱が主要因なら小規模実験 |
| 進捗スキーマ変更 | 学習履歴破損 | versionとmigrationを持つ | 読めないデータは説明付きで退避・初期化 |
| パッケージ更新 | 出力・API・教材記述の陳腐化 | CI再実行、版記録、定期監査 | 対応版を固定するか教材を更新 |
| Dropboxが`node_modules`をロック | Windowsローカルの`npm ci`が`EBUSY`で中断 | CIを正規のクリーン環境とし、可能なら開発用cloneを同期外に置く | 一時ディレクトリでlockfileを検証し、通常の`npm install`で復旧 |
| 一人の知識に依存 | 設計理由が失われる | ADR・正本・レビュー記録 | 暗黙判断を公開前に文書化 |
| 指標が正答率だけになる | 暗記を学習効果と誤認 | 転移・実技・遅延評価 | 達成表示を理解済みに限定 |
| ロードマップが再び分岐 | 公開状態・計画が不一致 | 構造化データから生成 | 手動複製を廃止するまで公開更新をレビュー必須化 |
| Stan構文を一度に詰め込む | 記号の暗記、認知負荷、後半での再学習が発生 | STEP 5から短い反復を分散し、前提単元と遅延再試行を固定 | 初学者観察で詰まりが集中した単元を分割し、同時提示する概念数を減らす |
| コンパイル成功をモデル妥当性と誤認する | 計算診断が良い誤モデルを報告する | 構文・計算・統計のエラー分類と、意図的な正答／誤答モデル比較 | 説明できない場合は推定結果の解釈へ進ませず、観測過程と尤度へ戻る |
| Stanの版更新で推奨構文や警告が変わる | 教材とコンパイラ出力が不一致 | 対応版を固定し、警告・非推奨構文・公式資料を定期監査 | 旧版の記憶を評価せず、移行差分を補講として分離する |

## 8. 直近の実行バックログ

| 順位 | ID | 作業 | 依存 | 状態 | 所有者 | 完了の証拠 |
|---:|---|---|---|---|---|---|
| 1 | FIX-001 | logical / NAの説明と問題を訂正 | なし | 完了 | Codex | 内容テストとR公式マニュアルを追加 |
| 2 | TEST-001 | React UIテスト基盤と初見判定の失敗テスト | なし | 完了 | Codex | 修正前に再現テスト2件が失敗することを確認 |
| 3 | FIX-002 | 初見判定を永続的なattempt状態へ移す | TEST-001 | 完了 | Codex | ページ移動・ホーム復帰を含むUIテストが成功 |
| 4 | FIX-003 | L10の外部URLをリンク化 | なし | 完了 | Codex | HTTPSリンクのhref・別タブ・relを自動検査 |
| 5 | FIX-004 | 現在公開中の範囲に文言を合わせる | なし | 完了 | Codex | README・ホーム・metaの公開範囲を整合 |
| 6 | FIX-005 | 因子水準を明示する | なし | 完了 | Codex | `levels` 明示を内容テストで固定 |
| 7 | TEST-002 | Rコード実行検証器 | M1 | 完了 | Codex | R 4.6.1の独立CI jobで全R検証が成功（run 31323415009） |
| 8 | OPS-001 | CI権限分離・SHA固定・PR実行 | なし | 完了 | Codex | SHA固定したNode・R・Stan jobがPRで成功し、deployはskip（run 31323415009） |
| 9 | UX-001 | 進捗スキーマv2とlocalStorage | D-003, TEST-001 | 完了 | Codex | 理解・実践進捗のreload・v1移行・破損保護・将来版保護・明示削除テスト成功 |
| 10 | UX-002 | URLルーティングとフォーカス管理 | TEST-001 | 完了 | Codex | deep link・未知URL・履歴・見出しfocus・キーボード完走テスト成功 |
| 11 | EDU-001 | L1–L10の目標―評価対応表 | D-001 | 完了 | Codex | 全10目標の文言・認知水準・次元・直接証拠を構造化し、完全被覆を自動検査 |
| 12 | EDU-002 | L10実践チェック | EDU-001 | 完了 | Codex | 5項目の実機完了条件、二層表示、再開・解除を含む永続化、axe検査を実装 |
| 13 | OBS-001 | Foundation Gate観察・判定プロトコル | EDU-001, EDU-002 | 実施準備完了・対象者観察待ち | Codex / 実施者 | 匿名記録票、T01–T07、支援水準、合否規則、機械可読状態、判定CLIを実装 |
| 14 | UX-003 | 実機アクセシビリティ監査 | UX-002 | 監査票完了・実機実行待ち | Codex / 実機監査者 | NVDA / VoiceOver、色コントラスト、モバイル実機の記録 |
| 15 | UX-PATH-001 | 初学者向け学習導線と再開規則 | D-005, UX-001, UX-002 | STEP 1まで拡張・対象者観察待ち | Codex / 実施者 | 体験→STEP 0→R基礎→Foundation Check→STEP 1、成果物未完了への再開、互換URL、状態遷移テスト |
| 16 | GOV-001 | 公開ロードマップ生成方式の設計 | M4 | 未着手 | 未割当 | 正本1つから生成可能 |
| 17 | STAN-001 | L34–L41の目標・依存・直接評価設計 | D-007, M4 | 非公開ドラフト完了 | Codex | `curriculum.json`の連番・逆依存・直接証拠を自動検査 |
| 18 | STAN-002 | 単回帰Stanコードの行単位解説 | STAN-001 | 非公開ドラフト完了 | Codex | 6ブロック、target、ベクトル化、診断、y_repを1本の原稿で接続 |
| 19 | STAN-003 | Stan原稿・実行コード同期検証器 | STAN-002 | 完了 | Codex | 原稿と`.stan` / `.R`の一致、事前分布・尤度・生成量・seed・実行証拠SHAを検査 |
| 20 | STAN-004 | 固定版CmdStanで構文・実行・診断証拠を取得 | STAN-003 | ローカル・CI実測完了、第三者フィードバック継続 | Codex / 協力者 | CmdStan 2.39.0で4 chain完走、診断・予測・限界を記録し、固定版CI jobも成功（run 31323415009） |
| 21 | EDU-003 | STEP 1データ操作とNB1 | D-007, D-010, M4 | 実装済み・対象者観察待ち | Codex / 実施者 | L11–L16、認知課題パイロットの分析依頼、CSV 5件・TSV 1件・区切りTXT 1件・Excel 3件、15ファイルの一括スターターZIP、完成版Rスクリプト、raw/issue/processed分離、条件差と解釈限界、再現可能なNB1、全6目標の直接証拠、R実行検証、成果物4項目 |
| 22 | OBS-002 | L12→L13データ品質観察 | EDU-003, UX-PATH-001 | 実施準備完了・対象者観察待ち | Codex / 実施者 | DQ01–DQ05、中立プロンプト、支援水準、raw fingerprint、未見表転移、匿名記録、単一変更の判断票を構造テストで固定 |
| 23 | EDU-004 | STEP 1独立転移課題 | EDU-003, M7 | 実装済み・対象者実施待ち | Codex / 実施者 | 列名・条件名・参加者数を変更し、完成コード非掲載、raw MD5固定、既知問題5件、検査済み16行、参加者別切替コスト80・100・100・110ms、3行メモを独立Rチェッカーで検査 |
| 24 | OBS-003 | STEP 1修了後の独立転移観察 | EDU-004, OBS-002, M7 | 実施準備完了・機械判定NOT RUN | Codex / 実施者 | TR01–TR05、参加者用課題と採点キーの分離、公式ヘルプ許可、初回提出固定、支援水準、自己修正、匿名記録、単一変更の判断票、観察用ZIPから提出後採点までのRリハーサル、3名・対象SHA・独立レビュー・未解決P0/P1ゼロの機械判定を固定 |
| 25 | EDU-005 | STEP 2順序・成果物・公開ゲート設計 | D-007, D-010, D-011, M4 | 非公開ドラフト完了 | Codex | L17–L20の意味ID、単一シナリオ、直列依存、2図・3CSV・探索メモ、4公開前提を構造検査 |
| 26 | EDU-006 | STEP 2 L17記述統計の縦切り | EDU-005 | 非公開実行検証済み | Codex | 固定seedの960試行、raw fingerprint、48参加者×条件行、3CSV・探索メモを一時Projectで再生成し、推測統計の先取りを拒否 |
| 27 | EDU-007 | STEP 2 L18作図文法の縦切り | EDU-005, EDU-006 | 非公開実行検証済み | Codex | L17の48行CSVを変更せず、固定jitter、2箱、48点、軸・単位・合成データ注記を持つ2100×1500px PNGを再生成 |
| 28 | EDU-008 | STEP 2 L17–L18内容理解問題 | EDU-006, EDU-007 | 非公開ドラフト完了 | Codex | 10問に目標次元、認知過程、誤答診断、再挑戦ヒント、記述rubric、解答例、未見転移を定義し、カリキュラム・原稿との同期を自動検査 |
| 29 | EDU-009 | STEP 2 L19参加者内対応図 | EDU-007, EDU-008 | 非公開実行検証済み | Codex | L17の48行CSVと24行差分CSVを変更せず、`group = id`で24本の対応線・48点・合成データ注記を持つ2100×1500px PNGを再生成。L19理解問題5問も追加 |
| 30 | EDU-010 | STEP 2 L20再現可能な最終分析パック | EDU-006, EDU-007, EDU-009 | 非公開実行検証済み | Codex | 1本の入口から2図・3CSV・探索メモを空の一時Projectで再生成し、入力不変、6成果物、7報告節、画像寸法、反復実行のバイト一致を検査。L20理解問題5問も追加 |
| 31 | OBS-004 | STEP 2初心者観察ゲート | EDU-010, OBS-003 | 実施準備完了・機械判定NOT RUN | Codex / 実施者 | S201–S205、未見のデバイス負荷データ、参加者用2ファイルZIP、完成コード・採点キー・チェッカー分離、初回提出固定、支援水準、自己修正、匿名記録、3名・対象SHA・独立レビュー・未解決P0/P1ゼロの機械判定を固定 |
| 32 | EDU-011 | L1–L16の段階的反復練習 | UX-PATH-001, EDU-003 | 実装済み・対象者観察待ち | Codex / 実施者 | 各レッスンで、まねる→1か所変える→例を閉じて想起→別文脈へ転移の4段階を順に解除。自己記録を理解・実践の証拠と分離し、schema v1・v2からv3へ進捗を移行 |
| 33 | STAN-005 | Stan分布文法スパイラルと切断ケース | STAN-001, STAN-003 | 非公開実行検証済み・第三者フィードバック継続 | Codex / 協力者 | ブロック、分布接尾辞、target、事前予測、ハイパーパラメータ、切断・打ち切りの6単元24課題。採用範囲のある測定器で正答・誤答3モデルを実行し、数式、Stan 2.39、R乱数、7枚のPNG、理論モーメント、8成果物を同期検査 |
| 34 | STAN-006 | リンク関数とPSIS-LOOモデル比較 | STAN-003, STAN-005 | 非公開実行検証済み・第三者フィードバック継続 | Codex / 協力者 | logit・probit・cloglog・Poisson-log、offset、係数尺度、比較可能性、pointwise log_lik、ELPD差、Pareto k、stackingの2単元8課題。線形・二次logitを4 chainで実行し、13成果物とSHA-256を同期検査 |
| 35 | STAN-007 | Stan Syntax Spine三層カバレッジと配置設計 | D-012, STAN-001, STAN-005, STAN-006 | 正本化・L34–L41受講用原稿実装完了、第三者フィードバック・pilot継続 | Codex / 協力者 | 言語・確率モデル・計算の三層、STEP 5〜L41の配置、6接触、3回の累積・遅延評価、6公開ゲートを`curriculum.json`と静的検証で固定。L34–L41は独立原稿8本・理解問題40問・目標次元・直接評価まで同期 |
| 36 | STAN-008 | 型・次元・ブロック・関数のエラーコーパス | STAN-007 | 非公開コア実装・固定版CI済み・第三者フィードバック継続 | Codex / Stan経験者 | セミコロン、array / vector、matrix / vector演算、添字型、スコープ、`_rng`許可ブロック、関数シグネチャ、Bernoulli観測型の壊れた例／修正版8組。固定版stanc3 2.39.0で失敗8・成功8、診断断片、16ソースSHA-256、転移課題、動的次元の検出限界を同期検査 |
| 37 | STAN-009 | コンパイル成功後のモデルレビューと計算文法 | STAN-007, STAN-008 | 非公開コア実装・L40実測完了、残り実測 / 第三者フィードバック継続 | Codex / 統計協力者 | Jacobian、Bernoulli-logit数値安定性、centered / non-centered、Poisson offset、pointwise log_lik、切断正規化のcandidate / reference 6組。全12ソースの構文証拠に加え、切断とL40弱・強情報比較は複数chain実測済み。残る数値極限・真値回収と第三者フィードバックを改善課題として追跡する |
| 38 | STAN-010 | 累積復習・遅延想起・未見転移 | STAN-007, STAN-009 | 非公開コア実装・pilot / 対象者観察待ち | Codex / 実施者 | L37後・L40後・L41後の3地点、10課題、6評価次元。初回／修正版／支援を分離し、L41後7〜14日の未見lognormal転移、facilitator参照モデル、匿名記録schema、pilot前は合格閾値未固定を構造検証。保持の実測は未実施 |
| 39 | OBS-005 | Stan構文初心者観察ゲート | STAN-008, STAN-009, STAN-010, M7 | 未着手 | 実施者 / 独立レビュー者 | 適格な初学者3名以上について、公式資料利用、初回エラー、支援水準、自己修正、正答コード、正しく動く誤モデルの発見、遅延転移を匿名記録し、未解決P0/P1ゼロで判定 |
| 40 | STAN-011 | Stan公開判定ゲート | STAN-004, STAN-007, STAN-008, STAN-009, STAN-010 | 必須6/6 PASS・ベータ公開承認済み | Codex / 所有者 | 対象`cbc6ef2`の静的検証、クリーンCI、既存runtime再検証、L40弱・強情報4-chain比較、公開範囲確認、最終判断が完了。Foundation、初学者観察、7〜14日後保持は非阻害の改善証拠として継続 |
| 41 | STAN-012 | Stan練習問題の反復増加 | STAN-010, STAN-011 | フィードバック記録済み・次候補で実装 | Codex | L34〜L41の既存40問・32段階課題を基準に、別時点で再訪する短い反復を追加し、到達目標対応、重複、認知負荷、静的検証を再確認する |

### 2026-08-10 Stan公開候補の固定とCI証拠

- 公開候補を`cbc6ef23b4b1c7154e83a54c0bc82a3a2f7edcff`へ固定し、クリーンなworktreeで`npm test`（24 files・397 tests）と`npm run test:stan-content`を再実行して`SRG02`を`PASS`にした
- 同じSHAへの[pull request run 31349208141](https://github.com/Ryuya-dot-com/Learning_Stan/actions/runs/31349208141)で`build`・`r-verify`・`stan-verify`がすべて成功したため、`SRG03`を`PASS`にした
- 同runのビルドと公開範囲自動監査も成功し、2026-08-10に所有者が全公開範囲を確認したため`SRG09`を`PASS`にした
- 所有者が既知の限界と問題一覧を確認してベータ公開可と判断したため`SRG10`も`PASS`となり、Stan Release Gateは必須6/6で`PASS`になった

### 2026-08-10 所有者判断と練習反復フィードバック

- リポジトリ所有者は対象`cbc6ef2`について、Stan L34〜L41、理解問題、合成データ、実行・検証証拠を公開してよいと確認した
- 学習アプリへのベータ公開も承認し、実際の導線有効化は別commitで実装・CI検証することにした
- 「練習問題の反復を増やしてほしい」という感想を匿名の`SRG06: RECORDED`として保存した
- 反復増加はP3の`SRG-IMPROVEMENT-001`として次の教材候補で扱い、現在の公開を止めない

### 2026-08-10 Stan公開ゲートの実用化

- ベータ公開を止める条件を`SRG02`〜`SRG05`・`SRG09`・`SRG10`の6項目へ限定した
- Foundation Gate、第三者フィードバック、初学者観察、遅延保持は非阻害の改善証拠へ移し、未実施の効果を主張しない契約にした
- `SRG06`は正式な審査や署名ではなく、口頭・チャット・文書の感想について日付、相手の属性、範囲、要点、対応を短く残す`RECORDED`状態に変更した
- `SRG09`は対象commitの自動監査とリポジトリ所有者の確認を要求し、第三者の署名を廃止した

### 2026-08-10 Stan公開範囲の自動監査

- Git管理対象の非公開観察path、credentialファイル、代表的なsecret、メールアドレス、実ユーザー名を含む絶対pathを検出する`npm run audit:stan-public-scope`を追加した
- ビルド後の通常assetへL34〜L41の教材ID・原稿が混入していないことを検査し、公開予定を示す`roadmap.html`だけを明示的に許可した
- CIのNode jobでビルド直後に監査を実行する。自動検査だけを公開許可へ格上げせず、対象commit、監査証拠、リポジトリ所有者の確認が揃った2026-08-10に`SRG09`を`PASS`へ更新した

### 2026-08-10 既存Stan runtime再検証

- Darwin arm64のR 4.6.1、CmdStanR 0.9.0、CmdStan 2.39.0、loo 2.10.1で単回帰、切断、リンク/LOOを一括再実行した
- 単回帰は最大R-hat 1.0011、切断は各chain 2,000 samplingで最大1.0029となり、両ケースの全chainでdivergence・最大treedepth到達0、E-BFMI 0.3以上を確認した
- リンク/LOOは線形`elpd_diff=-19.052`、`se_diff=5.574`、Pareto k閾値超過0、二次モデルstacking weight 0.999997で、予測比較の教材結論を再確認した
- CRLF/LF差でsource hashがOS依存にならないよう改行をLFへ正規化したcanonical SHA-256を採用し、3ケースの診断・教材結論・22成果物を保存証拠と静的検証へ結び付けた
- Stan Release Gateの`SRG04`を`PASS`へ更新した。この再検証時点では`SRG04`・`SRG05`の2/10が`PASS`で、残る証拠の記録前だった

### 2026-08-09 L40 centered / non-centered実測

- CmdStanR 0.9.0とCmdStan 2.39.0をDarwin arm64へ導入し、固定した弱情報（8群×1観測、真の`tau=0.1`）と強情報（8群×30観測、真の`tau=1`）で両表現を各4 chain・3反復実行した
- 性能runの全48 chain診断行を集約し、弱情報ではcentered / non-centeredのdivergenceが235 / 0、`tau` bulk ESS/sec中央値が341 / 16,233、強情報では0 / 0、27,624 / 3,500となった。弱情報はnon-centered、強情報はcenteredを選ぶ固定教材ケースとした
- 別の4 chain・sampling 2,000・`adapt_delta=0.99` runで`mu`・`tau`・`theta[1:8]`の20比較を行い、すべて4 combined MCSE以内、最大0.886 MCSEだった。一方で弱情報centeredには146 divergenceが残るため、平均一致だけを探索妥当性へ格上げしない
- 入力・環境・source hash・全chain診断・3反復時間・効率・事後同値性・2図を12成果物契約へまとめ、空の一時ディレクトリから再実行するR検証器と、改変を拒否する静的検証を追加した
- Stan Release Gateの`SRG05`を`PASS`へ更新した。当時の初版ゲートでは対象commitのクリーンCI、Foundation Gate、独立レビュー、初学者観察、遅延保持、公開範囲監査を別条件としていたため、全体判定は`BLOCKED`のままだった

### 2026-08-09 Stan公開判定ゲート（初版・現在は上記の実用化で改訂済み）

- L34〜L41の原稿完成と学習アプリ公開を分離し、当初は10証拠の`Stan Release Gate`を追加した。原稿完成やローカルテスト成功だけを公開許可へ格上げしない原則は現在も維持する
- 初版では`SRG01`で既存Foundation Gateの`PASS`を必須とし、対象commitの40桁SHAとHTTPS URL、クリーンCI、既存runtime証拠の再検証を要求した。2026-08-10の改訂でFoundation Gateは非阻害の改善証拠へ移した
- `SRG05`はL40のcentered / non-centeredを弱い群情報と強い群情報の両方で4 chain以上実測し、source hash、R・CmdStanR・CmdStan版、divergence、treedepth、E-BFMI、R-hat、ESS、MCSE、時間、事後同値性を記録したため、2026-08-09に`PASS`へ更新した
- 初版では主実装者と異なる独立レビュー、初学者3名以上、L41後7〜14日の保持3名以上を必須条件として構造化した。2026-08-10の改訂で、第三者の感想、初学者観察、遅延保持はいずれも非阻害の改善証拠へ移した
- secret・個人情報・アプリ公開範囲の監査と最終判断票を追加し、未解決P0・P1または明示的な失敗を`FAIL`、未実施・外部待ちを`BLOCKED`、管理情報のないP2を公開不可とした
- 入力済み観察記録、録画・録音、生ログ、直接識別子、未見variant、採点鍵は公開せず、GitHubには対象commit、環境、hash、匿名集約、問題、判断だけを置く契約を固定した

### 2026-08-09 L41受講用原稿

- 研究課題を対象母集団、観測単位、estimand、prediction taskへ分け、コード前protocolでデータ契約・生成過程・prior・停止規則・PPC統計量を固定する設計にした
- 構文成功、sampling終了、HMC診断、R-hat・ESS、MCSE、PPC、PSIS-LOOがそれぞれ直接支える範囲を分離し、段階ごとの解釈停止条件を追加した
- source、input、metadata、CSV、診断、予測、reportをrun IDと内容指紋で結ぶartifact manifestと、clean environmentでの第三者再実行手順を定義した
- seed・draw数・parameterizationの計算確認と、prior・likelihood・分析対象・prediction taskの実質的なsensitivity analysisを分類した
- 主要結果をclaim–evidence–limit表へ対応付け、予測順位をモデル真実性へ、計算診断を因果性・外的妥当性へ格上げしない報告契約にした
- L41原稿完成、学習者の卒業制作、L41後7〜14日の`sr41d`、Stan編のアプリ公開を四つの別状態として維持した
- 5問と3つの直接評価を追加し、原稿・カリキュラム・評価次元・必須説明の同期検査をL34–L41へ拡張した

### 2026-08-09 L40受講用原稿

- 階層標準偏差`tau`が0へ近づくと群効果が細い領域へ集中するfunnelを、場所により曲率と尺度が変わるHMCの問題として説明した
- `z ~ std_normal()`と`theta = mu + tau * z`から元の`theta | mu, tau`を導き、生成的non-centeringではmanual Jacobianを追加しない境界を明記した
- 弱い群情報ではnon-centered、強い群情報ではcenteredが有利になり得るため、一方を常時正解とせずデータ条件・診断・MCSE・ESS/secで選ぶ設計にした
- `adapt_delta`によるstep size変更を原因説明と区別し、divergence、E-BFMI、treedepth、R-hat、bulk/tail ESS、MCSE、時間を比較する順序を固定した
- 標準化ではprior尺度と元尺度への逆変換まで照合し、parameter制約、切断、打ち切り、prior・尤度変更を純粋な再パラメータ化から分離した
- 既存mr03の証拠をstanc3 2.39.0による両表現の構文成功・警告0件・ソースhashまでに限定し、教材の数値表を実測ではなく仮想診断と明示した
- 5問と3つの直接評価を追加し、原稿・カリキュラム・評価次元・必須説明の同期検査をL34–L40へ拡張した

### 2026-08-09 L39受講用原稿

- `generated quantities`が各posterior draw後に実行され、sampling済みparameterを変更しないことを、`log_lik`と`y_rep`の役割分離から説明した
- 観測値と各`y_rep`へ同じ平均・SD・最大値を適用し、二値回帰では全体成功率だけでなく予測子区間別の条件付きパターンを検査する手順を追加した
- コンパイル可能でも全尤度をN回複製する誤った`log_lik`を、観測ID・列順・除外単位から修正するデバッグ課題へ接続した
- 1行、新しい参加者、未来時点でholdout単位が異なることを明記し、同じ列数だけではモデル比較可能性を満たさない設計にした
- PSIS-LOOの標本数依存Pareto k閾値をELPDより先に確認し、高いkでは影響観測、モデル誤指定、moment matching、refit、K-foldを検討する順序を固定した
- 保存済み400観測・2モデルの実測から、既知の生成確率との予測曲線比較、Pareto k、`elpd_diff=-19.134`、`se_diff=5.601`を限定付きで判断し、未保存の`y_rep` PPCとstacking weightのモデル確率化を避けた
- 5問と3つの直接評価を追加し、原稿・カリキュラム・評価次元・必須説明の同期検査をL34–L39へ拡張した

### 2026-08-09 L38受講用原稿

- 推定値の解釈前に、対象run、divergence・E-BFMI、最大treedepth、R-hat、bulk・tail ESS、報告量のMCSEを確認する判断順序を固定した
- `$diagnostic_summary()`が返すchain別HMC診断と、`$summary()`・`posterior::mcse_quantile()`が返すparameter別診断を分離した
- divergenceは良好なR-hat・ESSで相殺せず実質的解釈を停止する一方、最大treedepth到達は主に効率問題として重大度と次の行動を分けた
- `R-hat < 1.01`、bulk・tail ESSの`100 × chain数`、E-BFMI 0.30という一般確認線を、研究目的に応じたMCSEの必要精度と区別した
- 保存済み単回帰の実測診断を教材内で判定しつつ、過去の証拠に未保存のMCSEを推測して追加しない証拠境界を明記した
- 計算診断が良い切断正規化なしモデルを反例に、指定した`target`の探索と観測モデルの妥当性を分け、L39の事後予測へ接続した
- 5問の理解問題と3つの直接評価を追加し、原稿・カリキュラム・評価次元・必須説明の同期検査をL34–L38へ拡張した

### 2026-08-09 L37受講用原稿

- 再現可能性をseedだけに限定せず、OS、R・CmdStanR・CmdStanの版とパス、Stanソース・入力JSONの指紋、chain ID、iteration、保存成果物、診断を1つのrunへ対応付けた
- `compile = FALSE`から`check_syntax(pedantic = TRUE)`、C++コンパイル、データ読込、4 chain samplingを別工程として扱い、各成功が保証する範囲を分けた
- `chains`と`parallel_chains`を区別し、単一seedがchain IDと組み合わされること、並列数を減らしてもchain数と保存draw数は減らないことを説明した
- 既定のCmdStan CSVが一時領域にあり得るため、`save_output_files()`または`output_dir`、`save_data_file()`、`save_object()`で永続成果物へ移す契約を追加した
- R側の事前検査、Stanの構文・コンパイル、実行時データ制約、サンプリング警告、保存失敗を停止工程から分類するデバッグ課題を追加した
- 5問の理解問題と、単回帰実行記録、データ契約修正、Bernoulli-logit独立実行の3直接評価をカリキュラム・原稿・静的検証へ接続した
- 既存の単回帰縦切り原稿も、暗黙の不適切一様事前分布と`~`・`..._lupdf`・`..._lpdf`の定数項の違いに合わせて訂正した

### 2026-08-09 L36受講用原稿

- 生成過程を、中心化、線形予測子、事前分布、正規尤度へ分解し、数式・Stanブロック・パラメータの単位を対応付けた
- distribution statementを代入や乱数生成と区別し、`~`と`target += ..._lupdf`の対応、正規化された`..._lpdf`との定数項の違いを明記した
- ベクトル化した正規尤度と観測ごとのloopを往復し、コードの短さと条件付き独立の仮定を別々に扱った
- 事前分布が欠けてもコンパイルし得る例を、暗黙の不適切一様事前分布、事後分布の適切性、事前予測・感度分析を確認するモデルレビュー課題へした
- 連続応答から0/1応答へ、整数配列、Bernoulli-logit尤度、logit係数、`inv_logit`による応答尺度への変換を移す転移課題を追加した
- 5問の理解問題と3つの直接評価を目標次元へ接続し、原稿・カリキュラム・問題ID・必須概念がずれた場合に静的検証を失敗させるよう拡張した

### 2026-08-09 L34–L35受講用原稿

- L34を独立した受講用原稿へ展開し、R、CmdStanR、stanc3、C++ツールチェーン、CmdStan、出力CSVの責務を分離した
- Rの名前付き`list`とStanの`data`ブロックを、名前・型・大きさ・値域で対応付け、長さ不一致・名前不一致・欠損判断をデータ契約として診断する手順を追加した
- `$check_syntax()`、`$compile()`、`$sample()`の成功が保証する範囲を分け、構文成功を統計的妥当性へ格上げしない説明を固定した
- L35を独立した受講用原稿へ展開し、Stanの7ブロックと教材単回帰例の主要6ブロックを区別した
- スコープ、`array`・`vector`・`row_vector`・`matrix`、次元・添字、data制約、parameter変換、事前分布、切断尤度を別概念として説明した
- L34–L35に各5問、合計10問の理解問題を追加し、選択、出力予測、記述、未見転移、誤答診断、再試行ヒント、rubric、モデル回答を構造化した
- 自己回答を実技証拠へ数えず、実行経路図、契約表、初回エラー、修正版、修正理由、白紙実装を直接証拠として残す設計にした
- カリキュラム、原稿、問題ID、到達目標次元、必須見出し、6接触、Stan公式資料への導線を`test:stan-content`で同期検査するよう拡張した

### 2026-08-05 設計更新

- D-012として、Stan構文をSTEP 5の生成コード読解からL41後の遅延転移まで続く必須系列に固定した
- `content/stan/curriculum.json`へ、言語・確率モデル・計算の三層、読解から遅延転移までの6接触、L37後・L40後・L41後の累積評価、6つの公開ゲートを構造化した
- 既存8単元32課題は実行可能な核として保持し、型・形状・スコープ・ユーザー定義関数、エラー修正、Jacobian、non-centered化、数値安定性をSTAN-008〜010で段階追加する
- STAN-008の非公開コアとして、主原因を1組1件に限定した壊れたStan例と修正版を8組追加。セミコロン、array / vector、matrix / vector演算、添字型、局所スコープ、`_rng`の許可ブロック、ユーザー定義関数、Bernoulli観測型を扱う
- Windows版stanc3 2.39.0へ`--warn-pedantic`を指定し、壊れた8例は期待するSyntax / Semantic errorと終了コード1、修正版8例は終了コード0を実測。診断の完全一致ではなく、分類・期待型・実際型・関数シグネチャの安定断片を検証する
- コーパス本体と16ソースのSHA-256、固定版、終了コード、限界を`syntax-error-validation.json`へ保存。ソース改変、転移課題欠落、三層・遅延評価欠落を静的検証で失敗させる
- 実測中にStan 2.39の予約語`offset`が意図したスコープ／関数エラーより先に停止することを検出し、各例が主原因1件だけを測るよう識別子を`shift`へ変更した
- 動的な行列次元はstanc3だけでは保証できないことを明記し、R側データ契約、実行時エラー、コンパイルは通る誤モデルをSTAN-009へ分離した
- STAN-009の非公開コアとして、Jacobian欠落、Bernoulli-logitの融合関数、centered / non-centered、Poisson exposure offset、観測別`log_lik`、切断正規化のcandidate / reference 6組を追加。全12ソースが固定版stanc3 2.39.0で終了コード0となることを確認した
- 判断を`candidate-reject`、`prefer-reference`、`context-dependent`に分離。Bernoulliの2表現を別モデルと誤認せず、centered表現を常に誤りとせず、データ情報量・数値安定性・計算診断で選ぶ設計を静的検証した
- Jacobian参照版だけがpedanticの「2 priors」警告を1件出し、Jacobian欠落版は警告0件だった。警告の有無を数学的正しさの代用にしない反例として、両方の警告数とソースSHA-256を保存した
- Poisson candidateは生の`exposure`をlogリンクへ足すため期待値が`exp(eta) * exp(exposure)`となる一方、referenceは`log(exposure)`により`exp(eta) * exposure`を表す。両者は型が同じでコンパイラには区別できないことを転移課題へ固定した
- LOO candidateは全データの対数尤度を各`log_lik[n]`へ複製し、referenceは`y[n]`と`eta[n]`の観測別寄与を保存する。配列長ではなく予測単位をレビューする契約を追加した
- 切断ケース以外は現段階で構文成功・意味差・SHA同期までであり、Jacobianの事前分布差、極端な線形予測子、階層モデル2条件、offsetの真値回収、LOO点別和の実測を公開前課題として残した
- STAN-010の非公開コアとして、L37後`sr37`、L40後`sr40`、L41後`sr41d`の3チェックポイントと10課題を追加。構文実行、エラー説明、数式とコードの往復、モデルレビュー、未見転移、遅延保持の6次元を0〜2の証拠rubricへ対応付けた
- `sr41d`はL41後7〜14日に、現行例で未使用の正の完了時間をlognormal回帰として白紙実装し、pointwise `log_lik`と`y_rep`まで含める。間隔はpilot前の暫定値で、最初の適格な初学者3名の保持率・支援量・完遂時間を見て再検討する
- pilot前に恣意的な合格率を置かず、rubric閾値を`not-fixed-before-pilot`に固定。自己チェックを保持証拠にせず、コード前計画、初回コード、初回コンパイラ結果、修正版、修正理由、支援水準、成果物hashを別記録とする
- 未見lognormal課題のfacilitator参照モデルを追加し、固定版stanc3 2.39.0で警告0・終了コード0を確認。計画、10課題、参照モデルのSHA-256を保存したが、これは学習者の遅延保持を測った証拠ではない
- 匿名記録schemaは直後・間隔あり・遅延を分離し、氏名、メール、所属、研究データを禁止フィールドとした。participant課題とfacilitator資料の物理分離、観察記録、合格閾値確定はOBS-005へ残す
- これは非公開範囲の設計更新であり、公開レッスン数、Foundation Gate、STEP 1・STEP 2観察状態を変更しない

### 2026-08-02 実行証拠

- Stan非公開パックを8単元32課題へ拡張。linkとinverse link、identity・logit・probit・cloglog・Poisson-log、曝露量offset、係数の尺度依存、比較可能性、PSIS-LOO、Pareto k、ELPD差と不確実性、stackingを数式・Stan・R・12問の内容理解問題で接続
- R 4.6.1 / CmdStanR 0.9.0 / CmdStan 2.39.0 / loo 2.10.1で、同じ400件の二値合成データへ線形・二次logitを各4 chain（warmup 750 + sampling 750）実行。ELPDは-231.412・-212.278、線形の差-19.134（SE 5.601）、Pareto k閾値超過0、divergence・最大treedepth到達0、二次モデルのstacking weight 0.9999997
- リンク・比較ケースは5枚のPNGと8 CSVを生成し、逆リンク基準値、基準確率で変わる確率差、Poisson offset、posterior予測曲線、pointwise ELPD、13成果物、5ソースのSHA-256を自動検査。5枚すべてを原寸目視し、初回検出したposterior drawの1列matrix展開バグを修正後に証拠を再取得
- リンク・比較ケース追加後の全22テストファイル・339テストとVite本番ビルドに成功。専用R実行検証も空の一時ディレクトリで2モデルを再コンパイルし、13成果物、ELPD差の点別合計、Pareto k、stacking、計算診断、教材ソース不変を確認
- 切断ケースを追加し、保存前の潜在値`Normal(0.15, 0.45)`から0〜1.5秒だけを残す2,000測定を固定seedで生成。正しい`T[L,U]`モデルは`mu=0.150`・`sigma=0.450`、入力制約だけの意図的な誤答は0.417・0.294を推定し、正規化項欠落の方向と大きさを実測
- 事前予測3条件をfixed-parameter samplerで各1,000回、正答・誤答モデルを各4 chain（warmup 1,000 + sampling 1,000）実行。両モデルともdivergence・最大treedepth到達0、E-BFMI 0.99以上、R-hat 1.01未満であり、「計算診断が良いことは観測過程の正しさを保証しない」という反例を固定
- 切断ケースは3枚のPNGと5 CSVを生成し、原稿・4 Stan例・2 R実行コード・2実行証拠のSHA-256、正答の真値回収、誤答の推定ずれ、両モデルの診断を`npm run test:stan-content`で自動検査。R 4.6.1専用ランタイムはCRAN公式MD5 `7907f3a20ec8ec88cd0da279024b8e27`を照合して隔離導入
- 切断ケース追加後の全22テストファイル・337テストとVite本番ビルドに成功。専用R実行検証も空の一時ディレクトリで3モデルを再コンパイルし、8成果物、真値回収、誤答のずれ、計算診断、教材ソース不変を確認
- Stan非公開パックの最初の6単元として、写経・変更・白紙再現・転移の24課題を追加。`_lpdf` / `_lpmf` / `_lcdf` / `_lccdf` / `_rng`、ベクトル化、ハイパーパラメータ感度、制約・切断・打ち切り・clampingの区別を数式とコードで接続
- `prior-predictive.stan`と`truncated-normal.stan`をStan 2.39.0の`stanc3 --warn-pedantic --info`で構文確認。R 4.5.1で正規・Beta・事前予測・切断の4枚のPNGを生成・目視確認し、6つの理論モーメントを固定seedのシミュレーションと許容差内で照合
- Stan文法ラボ追加後の全22ファイル・333テストとVite本番ビルドに成功。非公開ドラフトのため公開レッスン数とFoundation Gateの状態は変更しない
- L1〜L16の全16レッスンに、技能固有の4段階反復練習を追加。後半ほど完成例への依存を減らし、4段階完了後だけ理解問題へ進む一方向の足場を実装
- 4段階チェックは形成的な自己記録として`drill`へ保存し、理解済み・実践済み・観察済みの直接証拠には数えない。途中段階を解除すると後続段階も未完了へ戻す
- 進捗schemaをv3へ更新し、v1・v2の理解問題・誤答・実践進捗を保持した移行を追加。穴あき段階は先頭から連続した完了へ正規化する。レッスンデータ、進捗、キーボード完走、アクセシビリティ、既存品質ゲートを含む全22ファイル・331テストに成功
- 4段階反復統合後のVite本番ビルドに成功。接続可能なブラウザがないため実画面の目視確認は未実施であり、対象者観察時に表示密度と段階解除の理解を確認する

### 2026-08-01 実行証拠

- STEP 2初心者観察パック: 16デバイス×2条件×12測定の合成CSVを384行、SHA-256 `60ec20716259cb01eb2faf9e6f6de4d601a740042f312765db6f834445c56289`で固定。参加者ZIPを課題票とCSVの2ファイルだけに限定し、L17〜L20完成コード、採点キー、自己チェッカー、隠し期待値の非露出とZIPのバイト再生成一致を検査
- STEP 2観察運用リハーサル: ZIPを空の一時Projectへ展開し、初回提出時はチェッカーが存在しないこと、行順を変えた正しい3CSV・2図・7節メモの6成果物を固定した後だけチェッカーを追加して`STEP 2 TRANSFER PASS`になること、rawとZIPと提出物が不変であることをR 4.5.1で確認。これは運用証拠であり初心者理解の証拠には数えない
- STEP 2公開ゲート: Foundation、STEP 1データ品質、STEP 1独立転移に加え、`step2-learning-observation = OBSERVED`を必須化。現在状態はS201〜S205すべて`NOT RUN`、適格記録0/3であり、文書・リハーサルだけでは`OBSERVED`へ昇格しない
- STEP 2観察ゲート統合後の全回帰: 22ファイル・311テスト成功。本番Viteビルド成功。CIへ観察ZIP検査、`NOT RUN`状態整合、R採点動線リハーサルを追加
- STEP 2 L20実行検証: R 4.5.1 + ggplot2 4.0.3の空の一時Projectで`step2_report_and_transfer.R`だけを実行し、L17〜L19を順に呼び出して2図・3CSV・探索メモを再生成。raw入力のMD5不変、6成果物、由来・分析対象・2図の選択理由・観察・限界・再生成手順の7報告節、2図の2100×1500pxを確認し、2回目の実行でも6ファイルがバイト単位で一致
- STEP 2 L20理解問題: 7×5 inch・300 dpiの寸法予測、成果物が支える標本内記述、手作業Exportの監査、最終報告、未見の睡眠・覚醒度反復測定への転移を5問で検査。L17–L20は合計20問となり、再生成の成功と学習者の理解・転移を別証拠として扱う
- L20統合後の全回帰: 20ファイル・299テスト成功。本番Viteビルド成功。CIのR jobへ、単一入口からの6成果物再生成、7報告節、入力不変、反復実行一致を検査するL20実行検証を追加。Global Environmentへの再依存も負例で拒否
- STEP 2 L19実行検証: R 4.5.1 + ggplot2 4.0.3でL17成果物を再生成後に入力し、raw・48行要約CSV・24行差分CSVのMD5を維持したまま、`group = id`による24本の対応線・48点・差分CSVとの傾き完全一致・軸・ms単位・合成データ注記を持つ2100×1500px PNGを生成。画像を原寸目視し、文字切れ、対応関係の誤認を招く凡例、注記欠落がないことを確認
- STEP 2 L19理解問題: 参加者IDによるグループ化、24線・48点の事前予測、`group = id`欠落の診断、L18分布図との役割比較、未見の血圧データへの転移を5問で検査。L17–L19は合計15問となり、原稿・カリキュラム・評価の同期を自動検査
- L19統合後の全回帰: 20ファイル・295テスト成功。本番Viteビルド成功。CIのR jobへL17成果物不変、線・点の層順序、24グループ、48点、差分一致、PNG寸法の検査を追加
- STEP 2 L17–L18理解問題: 10問に到達目標次元、認知過程、証拠強度、選択肢ID、誤答診断、再挑戦ヒント、記述rubric、レビュー用解答例、未見転移を定義。問題数だけでなく、全次元被覆、正答ID、誤答診断、正答位置の単調化、rubric不足、原稿・カリキュラム直接証拠との不一致を自動拒否
- STEP 2 L18実行検証: R 4.5.1 + ggplot2 4.0.3でL17成果物を入力し、rawと48行CSVのMD5を維持したまま、2箱・完全重複しない48点・固定jitter・軸・ms単位・合成データ注記を持つ2100×1500px PNGを生成。画像を原寸目視し、文字切れ、色だけへの依存、注記欠落がないことを確認
- L18・理解問題統合後の全回帰: 20ファイル・292テスト成功。本番Viteビルド成功。CIのNode jobへ静的問題検証、R 4.6.1 jobへL18画像の層・点数・縦値不変・PNG寸法検査を追加
- STEP 2非公開パック: L17–L20を意味IDと暫定表示番号へ分離し、Foundation `PASS`・データ品質`OBSERVED`・独立転移`OBSERVED`・STEP 2初心者観察`OBSERVED`の4公開前提、直列依存、2図・3CSV・探索メモの最終契約を固定。24名×2条件×20試行の合成CSVはseed `20260802`、960行、SHA-256 `48d9da10bfad70a5777d537f6f1559a7c5017a017a4769fde4490eab4a4786db`で再生成一致
- STEP 2 L17実行検証: R 4.5.1のプロジェクト専用ライブラリで配布用`step2_descriptive.R`を空の一時Projectから実行し、raw MD5不変、884正答試行、48参加者×条件行、条件別2行、参加者内差24行、3CSV・4行探索メモの再生成に成功。これは実行可能性の証拠であり、初学者の理解・転移の証拠には数えない
- STEP 2統合後の全回帰: 19ファイル・284テスト成功。本番Viteビルド成功。初回全回帰で検出したWindows CRLFによるStan実行証拠SHAと負例置換の不一致は、読込時のLF正規化と`.gitattributes`契約で修正し、Stan 7件・STEP 2 12件を含む全回帰を再通過
- 公開ロードマップ整合: L17–L20を分析単位→図の文法→個人差→保存・限界・転移へ更新し、表示番号未確定の回帰ブリッジをSTEP 2とSTEP 3の間へ追加。構造テストと本番ビルドは成功したが、このセッションでは接続可能なブラウザがなく実画面の目視確認は未実施
- 公開前敵対的監査: STEP 1の合成データを「実データ」と見せる章名、L9・L10と検索向け説明に残った「STEP 1は今後公開予定」、自己申告チェックより強い修了時の能力断定を修正。現在の公開範囲・証拠水準・データ由来を固定する回帰を追加し、スターターZIPも同じ入力から再生成した
- 進捗互換性: 内容版を`2026-08-01-l1-l16-v3`へ更新。旧`l1-l10-v2`の理解・初見・誤答・実践進捗を保持したまま移行し、L1〜L16を正しく名乗るJSONへ再保存できることを検査
- 固定版Node検証: 公式Node 22.23.1 Windows ZIPのSHA-256`7df0bc9375723f4a86b3aa1b7cc73342423d9677a8df4538aca31a049e309c29`を再照合し、17ファイル・272テスト、Excel 3ブック、スターターZIP 15ファイル、観察用ZIP 3ファイル、本番ビルドがすべて成功
- 現変更のR互換検証: R 4.5.1で57件成功・27件理由付きmanual・失敗0件。ファイル入出力、NB1、完成版スクリプト、転移課題、観察リハーサルも一時Projectで成功し、正規版R 4.6.1の独立R jobでも[run 31323415009](https://github.com/Ryuya-dot-com/Learning_Stan/actions/runs/31323415009)により再検証した
- リリース安全性: 依存ツリー整合、秘密情報・秘密鍵・`.env`定型スキャン、固定メタデータを持つ生成物を確認し、`npm audit`は既知脆弱性0件。Foundation Gateは`BLOCKED`、STEP 1独立転移観察は`NOT RUN`のままで、対象者検証済みとは表示しない
- 独立転移観察の進行役Rリハーサル: R 4.5.1で3ファイルの観察用ZIPを一時Projectへ展開し、完成版・公開転移QMD・採点キー・自己チェッカーが初回提出前に存在しないことを確認。採点用fixtureの4成果物を固定した後だけチェッカーを追加し、`TRANSFER PASS`と`REHEARSAL PASS`、raw MD5・元ZIP MD5不変を確認。運用検査であり学習者証拠へ数えず、正規版R 4.6.1のCIでも成功
- 進行役Rリハーサル追加後の`npm test`: 17ファイル・268テスト成功。R 4.6.1独立jobへのリハーサル接続と、リハーサル結果を`OBSERVED`へ数えない文書契約を含む。`npm run build`も成功
- STEP 1独立転移観察の機械判定追加後の`npm test`: 17ファイル・267テスト成功。現在の`status.json`を正当な`NOT RUN`として検証し、対象SHA・TR01〜TR05の匿名証拠・適格記録3件以上・判断票・独立レビュー・未解決P0/P1ゼロが揃う場合だけ`OBSERVED`と判定。証拠なしの宣言、重複・欠落記録、管理されないP2、プロトコル汚染を拒否する回帰を含む
- `npm run gate:step1-transfer:status`: `NOT RUN`、OBSERVED 0/5、valid participants 0/3を終了コード0で報告。`npm run gate:step1-transfer:require-observed`は同じ状態を報告して終了コード1となり、観察完了を要求する運用だけを意図どおり停止
- STEP 1独立転移観察の機械判定追加後の`npm run build`: Vite本番ビルド成功。CIでは完了を偽装せず、状態JSONの妥当性を検査するstatusコマンドだけを実行
- STEP 1独立転移観察キット追加後の`npm test`: 16ファイル・259テスト成功。TR01–TR05、参加者用課題と期待値を持つ進行役資料の分離、自発的な公式ヘルプ利用、初回提出固定、支援水準、初回FAILからの自己修正、匿名記録、競合説明と単一変更の判断票を構造検査。既存のFoundation Gate、L12→L13 Data Quality Gate、学習導線、配布物、アクセシビリティを含む全回帰が成功
- 独立転移・観察用ZIP生成検証: JSZip 3.10.1で参加者用課題と転移CSV 2件だけの3ファイルを固定生成。完成版スクリプト、NB1、公開版転移QMD、採点キー、自己チェッカー、raw MD5、期待件数・期待値を含まず、元ファイルとのバイト一致、安全な相対パス、再生成ZIPの一致をCIで検査
- STEP 1独立転移観察キット追加後の`npm run build`: Vite本番ビルド成功
- STEP 1独立転移課題追加後の`npm test`: 15ファイル・251テスト成功。L11–L16を同じ分析依頼と固有作業で接続する契約、条件差を限定付きで説明する記述課題、成果物4項目、全16目標の直接証拠、レッスン別ダウンロードとリンク先実在、一括ZIPの固定マニフェスト・元ファイル一致・再生成一致、clean/dirty CSV・TSV・区切りTXT・Excelの内容契約、raw/issue/processed分離、NB1の処理一周、列名を変えた独立転移課題の解答非露出、OBS-002で完成版と自己チェッカーを事前露出しない契約、配色集約・コントラスト、成果物未完了への再開、主要STEP 1画面のaxe検査を含む
- STEP 1独立転移課題追加後の`npm run build`: Vite本番ビルド成功。CSV 5件・TSV 1件・区切りTXT 1件・Excel 3件、ケース対応`notebooks/nb1-data.qmd`、`scripts/step1_analysis.R`、`challenges/step1-transfer.qmd`、`challenges/step1_transfer_check.R`、`downloads/learning-stan-step1.zip`を配布物へ収録
- STEP 1一括スターター生成検証: JSZip 3.10.1を直接・固定依存とし、単一のProjectルート以下に案内文・NB1・完成版Rスクリプト・独立転移課題・自己チェッカー・全入力の15ファイルを収録。絶対パス・親参照を含まず、元ファイルとバイト単位で一致し、再生成ZIPも一致。`npm audit`で既知脆弱性0件
- STEP 1のR正規版ローカル検証: R 4.6.1、dplyr 1.2.1、readr 2.2.0、readxl 1.5.0、tidyr 1.3.2、tibble 3.3.1、purrr 1.2.2で56件成功・27件理由付きmanual・失敗0件。L13品質ゲート、単一Excel、複数Excelの完全コードを実ファイルで自動実行
- ケーススタディ追加後のR互換検証: R 4.5.1と固定版dplyr 1.2.1、readr 2.2.0、readxl 1.5.0、tidyr 1.3.2、tibble 3.3.1、purrr 1.2.2で57件成功・27件理由付きmanual・失敗0件。L16の参加者別incong−cong差55.05・80.65・104.55msと結果メモ生成を含み、正規版R 4.6.1のCIでも成功
- 完成版`step1_analysis.R`の一時Project実行smoke: R 4.5.1でraw MD5不変、既知問題5件、検査済み12行、参加者別条件差55.05・80.65・104.55ms、要約CSV、3行の限界付き結果メモを検査。`verify-step1-analysis.R`はR 4.6.1の独立CI jobでも成功
- 独立転移課題の一時Project実行smoke: R 4.5.1で転移rawのMD5不変、既知問題5件、検査済み16行・正答14行、参加者別切替コスト80・100・100・110ms、3行の限界付き結果メモを検査。課題文に完成処理を置かず、成果物だけを別Rチェッカーで判定し、R 4.6.1の独立CI jobでも成功
- ファイル入出力smoke: R 4.6.1でclean/dirty CSV、TSV、任意区切り、単一Excel、複数Excelの反復読込、元ファイル名付き結合、元CSVとの一致、3形式の書出しと再読込に成功
- NB1実行smoke: ケース追加後はR 4.5.1の`knitr::purl()`で全実行チャンクを一時Project内に再実行し、問題一覧5行、cleanと一致するcheckedデータ、参加者別要約CSV、3名全員で正の条件差、分析対象・記述結果・限界の3行メモを検査。現変更のR 4.6.1実行は独立CI jobで成功。Quarto 1.9.38のHTML生成は追加前の正規版ローカル検証で41段階を完走済み
- Excel生成検証: ExcelJS 4.4.0とuuid 11.1.1を固定し、3ブック・6シート・結合12試行、シート名・セル型・ヘッダー書式・固定行・元CSV一致を検査。全シートの目視確認と`npm audit`脆弱性0件を確認
- 初心者導線追加後の`npm test`: 14ファイル・180テスト成功。体験→STEP 0→R基礎→Foundation Check、全修了、前提未完了の実機確認、共有URLを検査
- 初心者導線追加後の`npm run build`: Vite本番ビルド成功
- Stan非公開ドラフト追加後の`npm test`: 13ファイル・170テスト成功
- `npm run test:stan-content`: L34–L41の連番・依存・直接評価、原稿と`.stan` / `.R`、実行証拠SHAの同期検査成功
- CmdStan実測: R 4.6.1 / CmdStanR 0.9.0 / CmdStan 2.39.0で構文確認・C++コンパイル・4 chain（warmup 1000 + sampling 1000）が成功。divergence 0、最大treedepth到達0、報告R-hat最大1.00、bulk ESS最小1778、tail ESS最小1705
- `npm ci --ignore-scripts`: 固定版Node 22.23.1 / npm 10.9.8、Dropbox外のクリーン一時環境で142パッケージ導入成功（Dropbox配下は同期ロックによる`EBUSY`を確認）
- `npm test`: 固定版Node 22.23.1 / npm 10.9.8で12ファイル・163テスト成功
- 初期範囲の`npm run test:r`: R 4.6.1で25件成功・16件理由付きmanual・失敗0件（現範囲の結果は上記56件・27件）
- `npm run build`: Vite本番ビルド成功
- `npm audit`: 既知の脆弱性0件
- Node/npm: 公式配布メタデータに基づき22.23.1 / 10.9.8へ固定し、CIの全Node jobが`.node-version`を参照
- Node公式Windows配布ZIP: 公式`SHASUMS256.txt`とSHA-256 `7df0bc9375723f4a86b3aa1b7cc73342423d9677a8df4538aca31a049e309c29`が一致
- 修正前の赤テスト: 初見判定の誤集計、HTTPS URLの非リンク表示をそれぞれ再現
- 修正後の回帰テスト: ページ移動とホーム復帰後も誤答履歴を保持し、一発クリアを2/3問と集計
- CI構造テスト: YAML構文、PR品質ゲート、独立R検証job、完全SHA、job単位の最小権限を検査
- M3回帰: reload、schema v1→v2移行、実践進捗の保存・解除、破損・将来版保護、明示削除、deep link、履歴、見出しfocus、キーボード完走を検査
- 教育整合: 全10目標の文言一致、直接証拠の全次元被覆、演習種別と評価方法、L10実践項目を自動検査
- アクセシビリティ: axe-core 4.12.1でホーム・レッスン・チートシート・独立したFoundation Check画面のserious / critical違反0件（jsdomで評価できない色コントラストはUX-003へ分離）
- Foundation Gate文書: 実機監査、匿名観察、T01–T07、支援水準、P0–P3、G01–G14、独立レビュー、未実施と合格の分離、相対リンクを6件の構造テストで固定
- Foundation Gate機械判定: 欠落・重複・破損・証拠なしPASS・未解決P0/P1・未管理P2・独立レビュー不在を8件の失敗系テストで拒否。現在はBLOCKED（PASS 0/14、実機・観察待ち）
- 配色: 実使用する30組の文字色・背景色を4.5:1、入力境界と主要背景上のフォーカス色8組を3:1で計算検査

## 9. 変更管理

- 重要方針の変更は、意思決定台帳に理由・代替案・再検討条件を残す
- マイルストーン完了時に、完了条件を証拠付きで確認する
- 「テストが通る」と「学習効果がある」を別の評価として扱う
- 未実装・構想・公開済みを同じ表現で混在させない
- 変更によって既存進捗が無効になる場合は、公開前に移行方針を示す
- 教材の正確性に関わる修正は、機能追加より優先する
