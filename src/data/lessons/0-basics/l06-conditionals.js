// レッスン: 条件分岐 — 場合によって処理を変える
// num は書かない（セクション順+ファイル名順から自動採番される）
export default {
  id: "l6",
  title: "条件分岐",
  tag: "場合によって処理を変える",
  pages: [
    {
      t: "もし〜なら",
      b: [
        "実験データを扱っていると、「反応時間が500ミリ秒より短ければ『速い』、そうでなければ『遅い』と表示したい」というように、値によって処理を変えたい場面がよく出てきます。こうした「もし〜なら、こうする」という分岐(ぶんき)を書くのが `if`(イフ、「もし」という意味の英語)です。",
        "`if` の基本の形は `if (条件) { 真のときの処理 } else { 偽のときの処理 }` です。`if` のあとのカッコに条件を書き、条件が成り立つ(TRUE)ときは最初の `{}` の中が、成り立たない(FALSE)ときは `else`(エルス、「そうでなければ」という意味)のあとの `{}` の中が実行されます。",
        "条件を書くときによく使うのが比較演算子(ひかくえんざんし)です。`<` は「より小さい」、`>` は「より大きい」、`==` は「等しい」、`!=` は「等しくない」を表します。L2で登場した代入の `<-` と、比較の `==` はよく似た見た目ですが、まったく別の意味です。`x <- 10` は「xに10を入れる」という代入、`x == 10` は「xは10と等しいか?」という比較でした。この2つを取りちがえないようにしましょう。",
        "このレッスンでは、条件によって処理を変えることができるようになります。",
      ],
    },
    {
      t: "実際に分岐させる",
      b: [
        "反応時間 `rt` が500より小さいかどうかで、表示する文字を変えてみましょう。",
      ],
      code: 'rt <- 620\nif (rt < 500) {\n  print("速い")\n} else {\n  print("遅い")\n}',
      out: '[1] "遅い"',
      a: [
        '`rt` には `620` が代入されているので、`rt < 500` は成り立ちません(FALSE)。そのため `else` のあとの `{}` が実行され、`print("遅い")` によって `[1] "遅い"` と表示されました。',
        "`{}` の中を複数行にわたって書くと、Rのコンソールでは2行目以降の先頭に `+` という継続プロンプトが表示されます。これは「まだ入力の途中ですよ」という合図で、自分で打つ必要はありません。`{` で始めたブロックが `}` で閉じられるまで、Rは入力の続きを待っています。",
      ],
    },
    {
      t: "ベクトルまるごと分岐する——ifelse",
      b: [
        "ここまでの `if` は、`rt` という1つの値に対する分岐でした。では、複数の参加者の反応時間をまとめたベクトルに対して、1人ずつ `if` を書かずに一気に分岐させたいときはどうすればよいでしょうか。そこで使うのが `ifelse`(イフエルス)です。",
        "`ifelse(条件, 真のときの値, 偽のときの値)` は、ベクトルの要素1つひとつに条件をあてはめ、それぞれに応じた値をまとめて返します。3人分の反応時間で試してみましょう。",
      ],
      code: 'rts <- c(430, 620, 380)\nifelse(rts < 500, "速い", "遅い")',
      out: '[1] "速い" "遅い" "速い"',
      a: [
        "`rts` の3つの値 `430, 620, 380` それぞれについて `< 500` かどうかが判定され、`速い`・`遅い`・`速い` という3つの結果がベクトルとして返ってきました。",
        "ここが重要な使い分けです。`if` は1つの値に対する分岐なので、ベクトルをそのまま渡すとエラーになり、実行はそこで止まります。試しに `rts` を `if (rts < 500) { ... }` に渡すと、`the condition has length > 1` というエラーが出ます。「条件の長さが1より大きい」、つまり値が複数あって1つに決められないという意味です。ベクトル全体をまとめて分岐させたいときは `ifelse` を使う、と覚えておきましょう。",
        "なお、エラーメッセージの見た目はPCの言語設定で変わります。英語環境では `Error in if (rts < 500) { : the condition has length > 1`、日本語環境では `if (rts < 500) { でエラー: the condition has length > 1` のように、前後の言い回しだけが訳されます。肝心の `the condition has length > 1` の部分は訳されず英語のまま出るので、この文字列を手がかりに読めば、どちらの環境でも同じエラーだと見分けられます。",
      ],
    },
  ],
  ex: [
    {
      id: "l6-q01", revision: 1, k: "choice",
      q: '`x <- 3` のとき `if (x > 5) print("大") else print("小")` の結果は?',
      opts: ['[1] "小"', '[1] "大"', "何も表示されない"],
      ans: 0,
      why: '`x` は `3` なので `x > 5` は成り立ちません(FALSE)。そのため `else` 側の `print("小")` が実行され、`[1] "小"` と表示されます。',
      hint: "`x` の中身が5より大きいかどうかを確かめてみましょう。",
    },
    {
      id: "l6-q02", revision: 1, k: "choice",
      q: "ベクトルの全要素を条件で振り分けるのはどちら?",
      opts: ["ifelse", "if", "else"],
      ans: 0,
      why: "`if` は1つの値に対する分岐です。ベクトル全体を一気に振り分けるには `ifelse` を使います。",
      hint: "`rts` のような複数の値をまとめて判定した命令は何でしたか。",
    },
    {
      id: "l6-q03", revision: 1, k: "tf",
      q: "次の3つの説明について、正しい(○)か、まちがっている(×)かを判定しましょう。",
      hint: "`==` と `<-` のちがい、`if` と `ifelse` の使い分けを思い出してみましょう。",
      items: [
        {
          s: "`==` は2つの値が等しいかを調べる",
          a: true,
          why: "そのとおりです。`==` は左と右が等しいかを確かめる比較演算子です。",
        },
        {
          s: "`if` はベクトルの全要素をまとめて判定できる",
          a: false,
          why: "`if` は1つの値に対する分岐です。ベクトルの全要素をまとめて判定したいときは `ifelse` を使います。",
        },
        {
          s: "`!=` は「等しくない」を意味する",
          a: true,
          why: "はい。`!=` は左と右が等しくないかどうかを確かめる比較演算子です。",
        },
      ],
    },
    {
      id: "l6-q04", revision: 1, k: "fill",
      q: "`rt` が500未満のときに `\"速い\"` を表示する条件を完成させましょう。",
      code: 'rt <- 450\nif (___) {\n  print("速い")\n} else {\n  print("遅い")\n}',
      verify: { mode: "manual", reason: "学習者が空欄を埋める未完成コードのため" },
      accept: ["rt < 500", "rt<500"],
      show: "rt < 500",
      why: "`rt < 500` は `rt` が500より小さいかを判定します。`450 < 500` は `TRUE` なので、最初の処理が実行されます。",
      hint: "「より小さい」を表す比較演算子を使います。",
    },
  ],
};
