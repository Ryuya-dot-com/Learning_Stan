// レッスン: データの型 — 数値・文字・論理
// num は書かない（セクション順+ファイル名順から自動採番される）
export default {
  id: "l3",
  title: "データの型",
  tag: "数値・文字・論理",
  pages: [
    {
      t: "3つの型",
      b: [
        "Rであつかう値には、いくつかの「型(かた)」があります。型とは、その値がどんな種類のデータかを表すラベルのようなものです。数値と文字ではできる計算やあつかい方がちがうので、Rは値ごとに型を区別しています。",
        "基本になるのは3つの型です。数値を表す numeric(ニューメリック)、文字を表す character(キャラクター)、そして「正しい・まちがい」を表す logical(ロジカル)です。",
        "numeric は `543.2` や `10` のような数字のデータで、四則演算ができます。character は `\"正答\"` のように `\"` で囲んだ文字のデータです。logical は `TRUE`(真)と `FALSE`(偽)で条件を満たすかどうかを表し、欠損(値がないこと)を示す `NA` も入ることがあります。`NA` は3つ目の真偽ではなく、欠損を表す印です。",
        "このレッスンでは、値の型を見分けることができるようになります。",
      ],
    },
    {
      t: "型を調べる",
      b: [
        "値の型は、`class`(クラス)という命令で調べられます。試しに、前のレッスンで使った `rt` の型を確認してみましょう。",
      ],
      code: "rt <- 543.2\nclass(rt)",
      out: '[1] "numeric"',
      a: [
        "`class(rt)` の結果は `[1] \"numeric\"` でした。`rt` には `543.2` という数字が入っているので、型は numeric です。結果が `\"` で囲まれているのは、`class()` が返す答えそのものが文字列(型の名前を表す文字)だからです。",
      ],
    },
    {
      t: "文字と論理",
      b: [
        "つづけて、文字列と論理値の型も確認してみましょう。",
      ],
      code: 'class("正答")\nclass(TRUE)\nclass(NA)',
      out: '[1] "character"\n[1] "logical"\n[1] "logical"',
      a: [
        "`\"正答\"` は `\"` で囲まれているので character、`TRUE` は `\"` をつけずに書くので logical です。型を指定せずに書いた `NA` も logical として扱われます。",
        "ここは間違えやすい点です。`TRUE` に `\"` をつけて `\"TRUE\"` と書いてしまうと、それは論理値ではなく、ただの文字列(character)になってしまいます。`TRUE` と `FALSE` は `\"` をつけずに書くのが決まりです。",
      ],
    },
  ],
  ex: [
    {
      id: "l3-q01", revision: 1, k: "choice",
      q: "`class(100)` の結果は?",
      opts: ['[1] "numeric"', '[1] "character"', '[1] "logical"'],
      ans: 0,
      why: "`100` は数字のデータなので、`class()` の結果は `[1] \"numeric\"` になります。",
      hint: "100は数字ですね。数字のデータの型は何と呼ばれていましたか?",
    },
    {
      id: "l3-q02", revision: 1, k: "choice",
      q: '`"TRUE"` と `TRUE` のちがいは?',
      opts: [
        "前者は文字列(character)、後者は論理値(logical)である",
        "前者は論理値(logical)、後者は文字列(character)である",
        "見た目がちがうだけで、意味は同じである",
      ],
      ans: 0,
      why: "`\"` で囲むと文字列(character)になります。`\"TRUE\"` は文字列、`\"` のない `TRUE` は論理値(logical)です。",
      hint: "`\"` で囲まれているかどうかに注目しましょう。",
    },
    {
      id: "l3-q03", revision: 1, k: "tf",
      q: "次の3つの説明について、正しい(○)か、まちがっている(×)かを判定しましょう。",
      hint: "型の名前(numeric・character・logical)と、それぞれどんな値かを思い出してみましょう。",
      items: [
        {
          s: "`class()` は値の型を調べる関数である",
          a: true,
          why: "そのとおりです。`class()` のかっこの中に値を入れると、その型が返ってきます。",
        },
        {
          s: '`"543.2"` は numeric である',
          a: false,
          why: "`\"` で囲むと文字列(character)になります。`543.2`(引用符なし)なら numeric ですが、`\"543.2\"`(引用符あり)は character です。",
        },
        {
          s: "logical のベクトルには TRUE・FALSE に加えて、欠損を表す NA も入る",
          a: true,
          why: "そのとおりです。`NA` は3つ目の真偽ではなく欠損を表す印ですが、型を指定しない `NA` は logical として扱われます。",
        },
      ],
    },
  ],
};
