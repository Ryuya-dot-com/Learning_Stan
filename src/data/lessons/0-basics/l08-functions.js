// レッスン: 関数 — 自分の道具をつくる
// num は書かない（セクション順+ファイル名順から自動採番される）
export default {
  id: "l8",
  title: "関数",
  tag: "自分の道具をつくる",
  pages: [
    {
      t: "関数を定義する",
      b: [
        "これまで `print`・`paste0` など、Rに最初から用意されている命令を使ってきました。こうした「名前がついていて、入力を渡すと、決まった処理をして結果を返す」しくみを関数(かんすう)と呼びます。ここまでは用意された関数を使うだけでしたが、このレッスンでは自分で関数をつくってみます。",
        "関数を定義するには `function`(ファンクション)というキーワードを使います。基本の形は `名前 <- function(引数) { 処理 }` です。値を代入するときと同じ `<-` を使って、`function(...)` でつくった関数に名前をつけます。カッコの中の引数(ひきすう)は、関数を呼び出すときに渡す入力の名前です。",
        "ここで重要な性質があります。Rの関数は、`{}` の中で最後に評価した式の結果が、そのまま関数の返り値になります。多くのプログラミング言語のように `return`(リターン)を書いて明示的に返す必要はありません。反応時間をミリ秒から秒に変換する `to_sec` という関数を作ってみましょう。",
        "このレッスンでは、自分で関数を作って呼び出すことができるようになります。",
      ],
      code: "to_sec <- function(ms) {\n  ms / 1000\n}\nto_sec(543.2)",
      out: "[1] 0.5432",
      a: [
        "`to_sec <- function(ms) { ms / 1000 }` で、引数 `ms` を1000で割るだけの関数を定義し、`to_sec` という名前をつけました。`to_sec(543.2)` は `ms` に `543.2` を渡して関数を呼び出しており、`{}` の中で最後に評価された `ms / 1000`(つまり `543.2 / 1000`)の結果、`[1] 0.5432` が返ってきます。",
        "`{}` の中に `return(ms / 1000)` と書いても同じ結果になりますが、Rでは最後の式がそのまま返り値になるので、`return` を省略するのが一般的な書き方です。この教材でもこれから `return` を書かない書き方で統一します。",
      ],
    },
    {
      t: "引数を複数とる",
      b: [
        "関数は複数の引数を受け取ることもできます。カッコの中に引数の名前を `,`(カンマ)で区切って並べます。",
        "ある値 `x` が、平均 `m` からどれだけ離れているかを、ばらつきの大きさ(標準偏差 `s`)を単位にして表した「zスコア」という値があります。心理学の実験データの標準化でよく使われる考え方で、詳しくはあとの統計編で扱いますが、ここでは「3つの引数を受け取って計算する関数」の例として `zscore` を作ってみましょう。",
        "関数を呼び出すときは、`zscore(620, 500, 100)` のように順番どおりに値を並べる(位置引数)だけでなく、`m = 500`・`s = 100` のように引数名を書いて渡す(名前付き引数)こともできます。名前をつけて渡すと、どの値がどの引数に対応するかが一目でわかり、読みまちがいを防げます。この書き方は、あとで登場する `mean(x, na.rm = TRUE)` のような命令を理解する土台になります。",
      ],
      code: "zscore <- function(x, m, s) {\n  (x - m) / s\n}\nzscore(620, m = 500, s = 100)",
      out: "[1] 1.2",
      a: [
        "`zscore <- function(x, m, s) { (x - m) / s }` で、3つの引数 `x`・`m`・`s` を受け取り `(x - m) / s` を計算する関数を定義しました。`zscore(620, m = 500, s = 100)` は `x` に `620`、`m` に `500`、`s` に `100` を渡しており、`(620 - 500) / 100` で `[1] 1.2` になります。",
        "ここでは `x` だけ位置(1番目の値)で渡し、`m` と `s` は名前を書いて渡しています。Rでは位置引数と名前付き引数を混ぜて書くこともできます。",
      ],
    },
    {
      t: "既定値をつける",
      b: [
        "関数の引数には、あらかじめ「既定値(きていち、デフォルト)」を設定しておくことができます。呼び出すときにその引数を省略すると、既定値がそのまま使われます。",
        "既定値をつけるには、関数を定義するときに `引数名 = 既定値` と書きます。参加者の名前に敬称をつける `greet` という関数を作ってみましょう。敬称 `suffix` の既定値は `\"さん\"` にしておきます。",
        "呼び出すときにその引数へ別の値を名前付きで渡すと、既定値を上書きできます。",
      ],
      code: 'greet <- function(name, suffix = "さん") {\n  paste0(name, suffix)\n}\ngreet("参加者A")\ngreet("参加者A", suffix = "様")',
      out: '[1] "参加者Aさん"\n[1] "参加者A様"',
      a: [
        '1つ目の `greet("参加者A")` では `suffix` を省略しているので、既定値の `"さん"` が使われ、`[1] "参加者Aさん"` になります。',
        '2つ目の `greet("参加者A", suffix = "様")` では、名前付き引数で `suffix` に `"様"` を指定しているので、既定値ではなく `"様"` が使われ、`[1] "参加者A様"` になります。',
        "この「既定値を持ち、名前付き引数で上書きできる」というしくみは、Rの多くの命令に共通しています。次のレッスンで登場する `mean(rt, na.rm = TRUE)` も、`na.rm` の既定値を名前付きで上書きしている書き方です。",
      ],
    },
  ],
  ex: [
    {
      id: "l8-q01", revision: 1, k: "choice",
      q: "`half <- function(x) { x / 2 }` のとき `half(10)` の結果は?",
      opts: ["[1] 5", "[1] 10", "[1] 2"],
      ans: 0,
      why: "`half` は引数 `x` を2で割る関数です。`half(10)` は `x` に `10` を渡すので `10 / 2` が計算され、`[1] 5` になります。",
      hint: "`{}` の中で最後に評価される式が返り値でした。`x` に `10` を入れて計算してみましょう。",
    },
    {
      id: "l8-q02", revision: 1, k: "fill",
      q: "関数を定義するキーワードの空欄を埋めましょう。",
      code: "square <- ___(x) {\n  x * x\n}",
      verify: { mode: "manual", reason: "学習者が空欄を埋める未完成コードのため" },
      accept: ["function"],
      show: "function",
      why: "`function` は関数を定義するときに使うキーワードです。`名前 <- function(引数) { 処理 }` の形で、名前に関数を代入します。",
      hint: "このレッスンの中心となるキーワードです。英語で「機能・働き」という意味の単語です。",
    },
    {
      id: "l8-q03", revision: 1, k: "tf",
      q: "次の3つの説明について、正しい(○)か、まちがっている(×)かを判定しましょう。",
      hint: "Rの関数の返り値の決まり方、既定値のはたらき、引数の数について思い出してみましょう。",
      items: [
        {
          s: "Rの関数は最後に評価した式が返り値になる",
          a: true,
          why: "そのとおりです。`{}` の中で最後に評価された式の結果が、`return` を書かなくても自動的に返り値になります。",
        },
        {
          s: "引数に既定値をつけると、呼び出し時に省略できる",
          a: true,
          why: "はい。`greet` の `suffix = \"さん\"` のように既定値をつけておくと、呼び出し時にその引数を省略でき、そのときは既定値が使われます。",
        },
        {
          s: "関数の引数は必ず1つでなければならない",
          a: false,
          why: "`zscore(x, m, s)` のように、関数は複数の引数を受け取ることができます。1つに限られているわけではありません。",
        },
      ],
    },
    {
      id: "l8-q04", revision: 1, k: "fill",
      q: "入力 `x` を2倍して返す関数の本体を完成させましょう。",
      code: "double <- function(x) {\n  ___\n}\ndouble(4)",
      verify: { mode: "manual", reason: "学習者が空欄を埋める未完成コードのため" },
      accept: ["x * 2", "x*2", "2 * x", "2*x"],
      show: "x * 2",
      why: "Rの関数は最後に評価した式を返します。`x * 2` を最後に置けば、`double(4)` は `8` を返します。",
      hint: "`return()` は不要です。引数 `x` を2倍する式だけを書きます。",
    },
  ],
};
