// レッスン: くり返しとベクトル化 — Rらしい書き方へ
// num は書かない（セクション順+ファイル名順から自動採番される）
export default {
  id: "l7",
  title: "くり返しとベクトル化",
  tag: "Rらしい書き方へ",
  pages: [
    {
      t: "同じ処理をくり返す",
      b: [
        "同じ処理を何度もくり返したい場面があります。たとえば `1` から `3` までの数をそれぞれ10倍して表示したいとき、1行ずつ `print(1 * 10)`・`print(2 * 10)`・`print(3 * 10)` と書くのは面倒ですし、数が増えるほど大変になります。こうした「同じ処理をくり返す」ためのしくみが `for`(フォー)文です。",
        "`for` の基本の形は `for (変数 in 値の並び) { 処理 }` です。指定した値の並びから1つずつ値を取り出して変数に入れながら、`{}` の中の処理を値の並びの数だけくり返します。",
        "値の並びをつくるときによく使うのが `:`(コロン)です。`1:3` は「1から3までの連番」を意味し、`c(1, 2, 3)` と同じ値のベクトルになります。`:` を使うと、連番を短く書けます。",
        "このレッスンでは、くり返し処理を書き、Rらしいベクトル化の書き方に置き換えることができるようになります。",
      ],
      code: "for (i in 1:3) {\n  print(i * 10)\n}",
      out: "[1] 10\n[1] 20\n[1] 30",
      a: [
        "`for (i in 1:3)` によって、`i` には `1`・`2`・`3` の値が1つずつ順番に入ります。それぞれの回で `{}` の中の `print(i * 10)` が実行され、`i` が `1` のときは `[1] 10`、`2` のときは `[1] 20`、`3` のときは `[1] 30` と、3回に分けて表示されます。",
        "L4のベクトルとちがい、`c(10, 20, 30)` のように結果が1つのベクトルにまとまるわけではなく、`print` が呼ばれるたびに `[1]` から始まる行が別々に表示される点に注意しましょう。",
      ],
    },
    {
      t: "合計を計算してみる",
      b: [
        "`for` を使うと、値を1つずつ足し合わせていく「合計」のような計算もできます。3人分のテストの得点 `80, 95, 72` の合計を、くり返し処理で計算してみましょう。",
        "まず `total <- 0` で、合計を入れておく変数を `0` で用意します。これを「累積変数(るいせきへんすう)」と呼びます。そして `for` の中で、得点が出てくるたびに `total <- total + s` として、それまでの `total` に新しい値 `s` を足しこんでいきます。",
      ],
      code: "total <- 0\nfor (s in c(80, 95, 72)) {\n  total <- total + s\n}\ntotal",
      out: "[1] 247",
      a: [
        "`total` は最初 `0` です。ループの1回目で `s` は `80` になり `total` は `0 + 80 = 80`、2回目で `s` は `95` になり `total` は `80 + 95 = 175`、3回目で `s` は `72` になり `total` は `175 + 72 = 247` になります。最後に `total` とだけ書いて実行すると、たまった中身 `[1] 247` が表示されます。",
        "このように、`0` からはじめて少しずつ値を足しこんでいく書き方は、合計を求める定番のパターンです。ただし、Rにはもっと簡単な方法があります。次のページで見てみましょう。",
      ],
    },
    {
      t: "Rでは、たいていループは要らない",
      b: [
        "多くのプログラミング言語では、合計を求めるのに `for` ループを書くのが基本です。しかしRには、ベクトル全体の合計を一度に計算してくれる `sum`(サム、「合計」という意味の英語)という命令があります。",
        "さきほどと同じ `80, 95, 72` の合計を、`sum` で求めてみましょう。",
      ],
      code: "sum(c(80, 95, 72))",
      out: "[1] 247",
      a: [
        "`total <- 0` から始めて `for` を何行も書かなくても、`sum(c(80, 95, 72))` の1行だけで、まったく同じ `[1] 247` という結果が得られました。",
        "Rには `sum` のほかにも、平均を求める `mean`(ミーン)、個数を数える `length`(レングス、L4で登場)など、ベクトル全体に対してまとめて計算してくれる命令(ベクトル化された関数)がたくさん用意されています。Rでは、こうしたベクトル化された関数をまず探し、それがないときだけ `for` を使う、という順番で考えるのが基本です。`for` はいわば「最後の手段」です。",
      ],
    },
  ],
  ex: [
    {
      k: "choice",
      q: "`for (i in 1:3) print(i)` は何回表示する?",
      opts: ["3回", "1回", "4回"],
      ans: 0,
      why: "`1:3` は `1`・`2`・`3` の3つの値からなる連番なので、ループは3回くり返され、`print(i)` も3回実行されます。",
      hint: "`1:3` が何個の値からなる連番か、数えてみましょう。",
    },
    {
      k: "choice",
      q: "`c(1, 2, 3, 4)` の合計を求めるRらしい書き方はどれでしょう?",
      opts: ["sum(c(1, 2, 3, 4))", "for (i in 1:4) i", "total <- 0"],
      ans: 0,
      why: "`sum(c(1, 2, 3, 4))` はベクトル全体の合計を1行で計算できる、Rらしいベクトル化された書き方です。`for` を書かなくても同じ結果が得られます。",
      hint: "このレッスンで学んだ、`for` を使わない求め方を思い出しましょう。",
    },
    {
      k: "tf",
      q: "次の3つの説明について、正しい(○)か、まちがっている(×)かを判定しましょう。",
      hint: "`sum` のようなベクトル化された関数と、`for` ループのちがいを思い出してみましょう。",
      items: [
        {
          s: "`1:5` は1から5までの連番を作る",
          a: true,
          why: "そのとおりです。`1:5` は `1, 2, 3, 4, 5` という連番のベクトルを作ります。",
        },
        {
          s: "Rでは合計を求めるのに必ず `for` が必要です",
          a: false,
          why: "`sum` という命令があるので、必ずしも `for` は必要ありません。ベクトル化された関数で済む場面がほとんどです。",
        },
        {
          s: "`for` の中の変数は1回ごとに次の値に変わる",
          a: true,
          why: "そのとおりです。`for (s in c(80, 95, 72))` なら、`s` は1回目に `80`、2回目に `95`、3回目に `72` というように、順番に値が変わっていきます。",
        },
      ],
    },
    {
      k: "fill",
      q: "1から3を順番に `i` へ入れる `for` 文の空欄を埋めましょう。",
      code: "for (___) {\n  print(i)\n}",
      verify: { mode: "manual", reason: "学習者が空欄を埋める未完成コードのため" },
      accept: ["i in 1:3", "i in 1 : 3"],
      show: "i in 1:3",
      why: "`for (i in 1:3)` と書くと、`i` に `1`・`2`・`3` が順番に入り、処理が3回実行されます。",
      hint: "基本形は `for (変数 in 値の並び)` です。",
    },
    {
      k: "fill",
      q: "`for` を使わず、`scores` 全体の合計を求める1行を書きましょう。",
      code: "scores <- c(80, 95, 72)\n___",
      verify: { mode: "manual", reason: "学習者が空欄を埋める未完成コードのため" },
      accept: ["sum(scores)"],
      show: "sum(scores)",
      why: "`sum(scores)` はベクトル全体の合計を直接計算します。累積変数と `for` を書く必要はありません。",
      hint: "合計を求める、3文字のベクトル化された関数を使います。",
    },
  ],
};
