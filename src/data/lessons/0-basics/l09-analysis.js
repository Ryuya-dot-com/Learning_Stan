// レッスン: データ解析に挑戦 — 基礎編のまとめ
// num は書かない（セクション順+ファイル名順から自動採番される）
export default {
  id: "l9",
  title: "データ解析に挑戦",
  tag: "基礎編のまとめ",
  pages: [
    {
      t: "表としてのデータ——data.frame",
      b: [
        "ここまでのレッスンでは、`c()` でつくる1種類の値だけのベクトルを扱ってきました。しかし実際の研究データは、参加者ID・実験条件・反応時間のように、複数の種類の情報が1試行(しこう)ごとに組になった「表」の形をしています。Rでこの表を表す型が `data.frame`(データフレーム)です。",
        "`data.frame` は `data.frame(列名 = ベクトル, 列名 = ベクトル, ...)` という形でつくります。それぞれの引数が1つの列になり、列名として渡した名前がそのまま見出しになります。渡すベクトルはすべて同じ長さでそろえる必要があります。",
        "2人の参加者(P01・P02)が、それぞれ一致(cong)条件と不一致(incong)条件で反応時間を計測したデータを、表にまとめてみましょう。",
        "このレッスンでは、表形式のデータから平均や標準偏差を求めることができるようになります。",
      ],
      code: 'd <- data.frame(\n  id = c("P01", "P01", "P02", "P02"),\n  cond = c("cong", "incong", "cong", "incong"),\n  rt = c(512.5, 560.4, 489.9, 603.1)\n)\nd\nnrow(d)',
      out: "   id   cond    rt\n1 P01   cong 512.5\n2 P01 incong 560.4\n3 P02   cong 489.9\n4 P02 incong 603.1\n[1] 4",
      a: [
        "`id`・`cond`・`rt` という3つの列を持つ、4行の表ができました。1行が1試行(P01さんのcong条件、P01さんのincong条件、…)に対応しています。表示された `d` の先頭行にある `id cond rt` は列名、左端の `1`〜`4` は行番号です。",
        "`c()` でベクトルをつくる書き方(L4)が、そのまま `data.frame` の中でも使われている点に注目しましょう。`data.frame` は、複数のベクトルを列としてまとめて1つの表にするしくみです。",
        "`nrow(d)` で表の行数、つまり「データが何件あるか」を調べられます。`d` は4行なので `[1] 4` が返されました。",
      ],
    },
    {
      t: "列を取り出して集計する",
      b: [
        "表の中から特定の1つの列だけを取り出したいときは `$`(ドル記号)を使います。`d$rt` と書くと「表 `d` の `rt` 列」が取り出せます。`$` は「この表のこの列」を指す記号だと覚えておきましょう。",
        "列を取り出すと、中身はただの数値ベクトルになるので、L7で登場した `mean`(平均)や、ばらつきの大きさを表す `sd`(エスディー、standard deviationの略、標準偏差)がそのまま使えます。",
      ],
      code: "mean(d$rt)\nsd(d$rt)",
      out: "[1] 541.475\n[1] 50.51511",
      verify: { mode: "numeric", absoluteTolerance: 1e-6, relativeTolerance: 1e-9 },
      a: [
        "`mean(d$rt)` は `d` の `rt` 列(4つの反応時間)の平均を計算し、`[1] 541.475` になりました。`sd(d$rt)` は同じ4つの値の標準偏差を計算し、`[1] 50.51511` です。",
        "`d$rt` のように `表$列名` と書くだけで、その列を1本のベクトルとして取り出せます。これで、ベクトルに使えた命令が、表の1列にもそのまま使えるようになりました。",
      ],
    },
    {
      t: "欠損値 NA との付き合い方",
      b: [
        "実際の研究データでは、参加者が回答しなかった、記録に失敗した、外れ値を除いたなどの理由で、値が欠けていることがよくあります。Rはこの「値がない」状態を `NA`(エヌエー、Not Availableの略、欠損値)という特別な値で表します。",
        "欠損値があるとき、平均を求める `mean` はどう振る舞うでしょうか。3つの値のうち2つ目が `NA` であるベクトル `rt2` で試してみましょう。",
      ],
      code: "rt2 <- c(512.5, NA, 489.9)\nmean(rt2)\nmean(rt2, na.rm = TRUE)\nis.na(rt2)\nsum(is.na(rt2))",
      out: "[1] NA\n[1] 501.2\n[1] FALSE  TRUE FALSE\n[1] 1",
      a: [
        "`rt2` には `NA` が1つ含まれています。`mean(rt2)` は `[1] NA` を返しました。ベクトルの中に `NA` が1つでもあると、Rは「一部の値がわからないので、全体の平均も計算できない」とみなし、結果を `NA` にします。値の数が多くても、1つでも `NA` が混ざっていれば結果は `NA` になる点に注意しましょう。",
        "欠損値を除いて残りの値だけで平均を計算したいときは、L8で学んだ名前付き引数を使って `mean(rt2, na.rm = TRUE)` と書きます。`na.rm`(エヌエー・リムーブ、`NA` を取り除くという意味の引数)を `TRUE` にすると、`NA` を除いた `512.5` と `489.9` の2つだけで平均が計算され、`[1] 501.2` になります。既定では `na.rm` は `FALSE` なので、指定しないと `NA` はそのまま結果に残ります。",
        "`is.na` は要素ごとに欠損かどうかを `TRUE`/`FALSE` で返します。`rt2` の2番目が `NA` なので `[1] FALSE  TRUE FALSE` が返されました。`sum` と組み合わせると欠損の個数が数えられます。`TRUE` が 1 として足されるので、欠損が1つあれば `[1] 1` になります。",
      ],
    },
    {
      t: "条件ごとに平均を出す",
      b: [
        "最後に、ここまで学んだ道具を組み合わせて、「cong条件だけの反応時間の平均」を求めてみましょう。ここで合流するのは、L4の `[]`(取り出し)、L6の `==`(比較)、そしてこのレッスンの `$`(列の取り出し)です。",
        '`d$cond == "cong"` は、表 `d` の `cond` 列の各値が `"cong"` と等しいかどうかを、TRUE/FALSEのベクトルとして返します(L6)。このTRUE/FALSEのベクトルを `d$rt[...]` のように `[]` の中に入れると(L4)、TRUEに対応する `rt` の値だけを取り出せます。',
      ],
      code: 'mean(d$rt[d$cond == "cong"])\nround(mean(d$rt), 1)',
      out: "[1] 501.2\n[1] 541.5",
      a: [
        '内側から順に見ていきましょう。`d$cond == "cong"` は `d` の4行それぞれについて `cond` が `"cong"` かどうかを判定し、`TRUE FALSE TRUE FALSE` という4つの論理値を返します(1・3行目がcong)。これを `[]` の中に入れた `d$rt[c(TRUE, FALSE, TRUE, FALSE)]` は、`rt` 列からTRUEの位置、つまり1行目と3行目の値 `512.5` と `489.9` だけを取り出します。最後に `mean` で平均を取り、`[1] 501.2` になりました。',
        "`round(mean(d$rt), 1)` は全体の反応時間の平均を小数第1位に丸めて `[1] 541.5` になります。論文や発表で数値を示すときに使う便利な命令です。",
        "`$` で列を選び、`==` で条件をつくり、`[]` で絞り込んで、`mean` で集計する——この一連の流れが、Rでデータを分析する基本の型です。今後公開予定のSTEP 1(データ操作編)では、`group_by` と `summarise` という命令を使って、この計算をもっと読みやすく書けるようになります。まずは次のFoundation Checkで、ここまでの操作を自分のPCで再現できるか確かめましょう。",
      ],
    },
  ],
  ex: [
    {
      k: "choice",
      q: "`d$rt` は何を取り出す?",
      opts: ["表 d の rt 列", "表 d の rt 行", "rt という名前の表"],
      ans: 0,
      why: "`$` は「この表のこの列」を指す記号です。`d$rt` は表 `d` の `rt` 列だけを取り出します。",
      hint: "`$` が何を指す記号だったか思い出しましょう。",
    },
    {
      k: "choice",
      q: "`mean(c(1, NA, 3))` の結果は?",
      opts: ["[1] NA", "[1] 2", "[1] 4"],
      ans: 0,
      why: "ベクトルに1つでも `NA`(欠損値)が含まれていると、`mean` は既定では計算できず `[1] NA` を返します。除いて計算したいときは `na.rm = TRUE` を指定します。",
      hint: "欠損値が1つでも混ざっていると、既定の `mean` はどうなるでしょうか。",
    },
    {
      k: "tf",
      q: "次の3つの説明について、正しい(○)か、まちがっている(×)かを判定しましょう。",
      hint: "`na.rm` のはたらきと、欠損値が混ざったときの `mean` の既定の挙動を思い出してみましょう。",
      items: [
        {
          s: "`na.rm = TRUE` は欠損値を除いて計算する",
          a: true,
          why: "そのとおりです。`na.rm = TRUE` を指定すると、`NA` を除いた残りの値だけで計算します。",
        },
        {
          s: "欠損が1つあっても `mean` は残りの値で平均を出す",
          a: false,
          why: "既定(`na.rm = FALSE`)では、欠損が1つでもあると `mean` は `NA` を返します。残りの値だけで自動的に平均を出すわけではありません。",
        },
        {
          s: "`sd()` は標準偏差を計算する",
          a: true,
          why: "はい。`sd` は standard deviation(標準偏差)の略で、値のばらつきの大きさを計算します。",
        },
      ],
    },
    {
      k: "fill",
      q: "表 `d` の `rt` 列について、欠損値を除いて平均を求めるコードを書きましょう。",
      code: "___",
      verify: { mode: "manual", reason: "学習者が空欄を埋める未完成コードのため" },
      accept: ["mean(d$rt, na.rm = true)", "mean(d$rt,na.rm=true)", "mean(d$rt, na.rm=true)"],
      show: "mean(d$rt, na.rm = TRUE)",
      why: "`d$rt` で列を取り出し、`na.rm = TRUE` を指定すると、欠損値を除いた平均を計算できます。",
      hint: "`mean` の第1引数に `d$rt`、名前付き引数に `na.rm = TRUE` を渡します。",
    },
    {
      k: "fill",
      q: "表 `d` の `rt` 列の標準偏差を求めるコードを書きましょう。",
      code: "___",
      verify: { mode: "manual", reason: "学習者が空欄を埋める未完成コードのため" },
      accept: ["sd(d$rt)"],
      show: "sd(d$rt)",
      why: "`sd(d$rt)` は、表 `d` から取り出した `rt` 列の標準偏差を計算します。",
      hint: "standard deviationを略した2文字の関数を使います。",
    },
  ],
};
