// STEP 1: tidyverseとパイプ
export default {
  id: "l11",
  title: "tidyverseとパイプ",
  tag: "処理を上から順に読む",
  pages: [
    {
      t: "STEP 1で使う道具",
      b: [
        "STEP 1では、研究データの読み込みと整形に強いパッケージ群 `tidyverse` を使います。tidyverseは1つの巨大な命令ではなく、表を操作する `dplyr`、CSVを読む `readr`、表の形を変える `tidyr` など、役割の異なるパッケージをまとめた名前です。",
        "RStudioを起動し直した後は、使うパッケージを `library()` で読み込みます。ここでは表操作を担当する `dplyr` を読み込み、L9の `data.frame` に似た `tibble` を作ります。tibbleはデータフレームの一種で、列型などを読みやすく表示するよう設計されています。",
        "このレッスンでは、パイプを使って、データに対する複数の処理を上から順に書くことができるようになります。",
      ],
      code: `library(dplyr)
d <- tibble::tibble(
  id = c("P01", "P01", "P02", "P02"),
  cond = c("cong", "incong", "cong", "incong"),
  rt = c(512.5, 560.4, 489.9, 603.1)
)
c(nrow(d), ncol(d))`,
      out: "[1] 4 3",
      a: [
        "`library(dplyr)` は、インストール済みのdplyrを今回のRセッションで使えるようにします。`install.packages()` はここでは繰り返しません。",
        "作った表 `d` は4行3列です。以降の例では、左端にある表 `d` を少しずつ加工していきます。",
      ],
    },
    {
      t: "パイプは左の結果を右へ渡す",
      b: [
        "`|>` はパイプと呼ばれるR標準の記号です。左側で得た値を、右側の関数の第1引数へ渡します。処理結果を一度ずつ別の変数へ保存しなくても、作業の流れを上から下へ書けます。",
        "まずは表ではなく数値で、動きを確かめます。左側のベクトルが `mean()` の第1引数へ渡されます。",
      ],
      code: `c(512.5, 560.4, 489.9) |>
  mean()`,
      out: "[1] 520.9333",
      verify: { mode: "numeric", absoluteTolerance: 1e-4, relativeTolerance: 1e-8 },
      a: [
        "これは `mean(c(512.5, 560.4, 489.9))` と同じ計算です。パイプでは、入力が先、処理が後に現れるため、「このデータを、平均する」と読むことができます。",
        "パイプの右側には関数呼び出しを書きます。`mean` だけでなく `mean()` とカッコまで書く点に注意してください。",
      ],
    },
    {
      t: "表を絞って、列を取り出す",
      b: [
        "dplyrの `filter()` は、条件を満たす行だけを残します。続く `pull()` は、指定した1列をベクトルとして取り出します。パイプを使うと「表dから、cong行を残し、rt列を取り出す」と上から読めます。",
      ],
      code: `d |>
  dplyr::filter(cond == "cong") |>
  dplyr::pull(rt)`,
      out: "[1] 512.5 489.9",
      a: [
        "`filter()` の中では `d$cond` ではなく、列名 `cond` を直接書けます。表 `d` がパイプで第1引数へ渡されているため、dplyrはその表の列名だと判断できます。",
        "`dplyr::filter` の `::` は「dplyrパッケージにあるfilter」という明示です。`library(dplyr)` の後なら `filter()` だけでも動きますが、どのパッケージの関数かをはっきり示したい例では `::` が役立ちます。",
      ],
    },
    {
      t: "入れ子とパイプを読み比べる",
      b: [
        "パイプを使わず同じ処理を書くと、関数の内側から外側へ読む入れ子になります。短い処理なら問題ありませんが、操作が増えるほど、実行順と読む順が逆になって追いにくくなります。",
      ],
      code: `dplyr::pull(
  dplyr::filter(d, cond == "incong"),
  rt
)`,
      out: "[1] 560.4 603.1",
      a: [
        "実行順は `filter()` が先、`pull()` が後です。パイプなら `d |> filter(...) |> pull(rt)` となり、書かれた順と実行順が一致します。",
        "ただし、パイプを長くつなげれば必ず読みやすいわけではありません。1つの処理が何十行にもなる場合や、途中結果を後でも使う場合は、意味のある名前を付けて保存する方が安全です。",
      ],
    },
  ],
  ex: [
    {
      k: "choice",
      q: "`x |> mean()` と同じ意味になるコードはどれですか?",
      opts: ["mean(x)", "x(mean)", "mean() |> x"],
      ans: 0,
      why: "パイプは左側の `x` を右側の `mean()` の第1引数へ渡すため、`mean(x)` と同じです。",
      hint: "左側の値は、右側の関数のどこへ入るでしょうか。",
    },
    {
      k: "fill",
      q: "左側のデータを右側の処理へ渡す、R標準のパイプ記号を入力してください。",
      code: "d ___ dplyr::filter(cond == \"cong\")",
      verify: { mode: "manual", reason: "学習者がパイプ記号を補う未完成コードのため" },
      accept: ["|>"],
      show: "|>",
      why: "`|>` は左側で得た値を、右側の関数の第1引数へ渡します。",
      hint: "縦線、より大きい記号の順に並ぶ2文字です。",
    },
    {
      k: "tf",
      q: "パイプについて、正しい(○)か、まちがっている(×)かを判定しましょう。",
      hint: "値が渡される方向と、libraryの役割を分けて考えます。",
      items: [
        { s: "`x |> f()` は、通常 `f(x)` と同じ順序で処理する", a: true, why: "左側のxが右側のfの第1引数へ渡されます。" },
        { s: "パイプを使うには、毎回tidyverseを再インストールする", a: false, why: "`|>` はR標準の記号です。パッケージのインストールも毎回は不要です。" },
        { s: "パイプは、データから処理へ上から順に読める", a: true, why: "入力を先に書き、その後へ処理をつなげられるのが利点です。" },
      ],
    },
  ],
};
